# HeteroNetwork Architecture

## Goal

Build a production-oriented Rust P2P VPN / overlay network where one or more globally reachable nodes provide bootstrap, control-plane, signal, STUN, and relay capability. Nodes behind NAT join with signed tokens. Data traffic uses direct peer paths whenever possible and falls back to relay only when direct public, IPv6, or NAT-traversed UDP paths are unavailable.

The data plane must continue after the control plane is unavailable. New joins, peer-map updates, policy changes, and route changes may depend on the control plane.

## Required Components

- `control-plane`: registration, token validation, VPN IP allocation, peer map, relay map, ACL, route, policy, health, and path-state management.
- `signal`: endpoint candidate exchange, UDP hole-punch coordination, and path negotiation.
- `agent`: identity/WireGuard key management, STUN probing, candidate collection, lazy connect, peer-map application, peer probing, WireGuard endpoint updates, route management, and health reports.
- `relay`: UDP relay that forwards opaque E2E-encrypted packets and cannot decrypt payload.
- `stun`: public endpoint detection for NAT traversal.
- `route-manager`: Linux routing, policy routing, Docker integration, and Kubernetes node-underlay integration.
- `cli`: entry point for cluster init, joins, agent/control-plane status, peer/route queries, path/relay inspection, and typed Docker/Kubernetes install plans.

## Trust And Identity

Each node has two key families:

- Identity key: Ed25519 signing key used for node identity, token issuing, and control-plane authentication.
- WireGuard keypair: X25519 key material used by kernel WireGuard for data-plane encryption.

The node ID is derived from the identity verifying key. WireGuard keys can rotate independently from identity keys. Join tokens contain:

- cluster ID
- bootstrap endpoints
- expiration and not-before time
- role and tags
- token policy
- issuer node ID and key ID
- Ed25519 signature over typed claims

Operators can keep the issuer private key outside the control-plane process and pass only the issuer node ID, key ID, and public key to `iparsd control-plane`. Additional trusted issuer public keys can be supplied with repeated `--trusted-issuer-key issuer_node_id,key_id,public_key` values, or semicolon-separated `HETERONETWORK_TRUSTED_ISSUER_KEYS`, so key rotation can overlap old and next signing keys without stopping registration. `ipars init` can generate and persist the issuer private key with restrictive file permissions, emit the concrete `iparsd` commands for control-plane, signal, STUN, and relay bootstrap services, and optionally spawn those services with inherited environment variables cleared, a fixed system `PATH` plus `C` locale, per-service logs under owner-only state/log directories, and owner-only non-symlink, non-hardlinked log files. It validates the public endpoint and listener ports used to derive bootstrap URLs before signing the initial token. `ipars token create` can later sign additional join tokens with that same key or the next key ID after rotation, including typed and scheme-validated control-plane, signal, STUN, and relay bootstrap endpoints. Numeric bootstrap hosts are checked before signing so port-zero, unspecified, multicast, and IPv4 broadcast socket addresses cannot be embedded into join tokens. Both commands expose token-policy inputs for relay permission, route allowlists, and max-use or unlimited-use admission.

Token ledger use is recorded in durable state before registration is accepted. Definition initialization, revocation lookup, status/max-use validation, and use consumption are one serialized operation; existing rows are never reset during first use or re-issuance. SQLite serializes with its writer transaction, while PostgreSQL admission and revocation share a transaction-scoped advisory lock derived from cluster/nonce. New records retain the complete signed claims and reject a cluster/nonce collision with a different definition. A separate durable revocation tombstone can be created before claims are first observed; admission materializes the token record as revoked without consuming a use. Tombstone-only entries count as revoked ledger tokens in metrics, and the tombstone plus an optional full token record form the revocation result.

During registration, the control plane validates that the submitted identity public key is valid Ed25519 key material, requires the submitted node ID to match the ID derived from that key, validates the submitted WireGuard public key as base64-encoded 32-byte key material, and rejects endpoint candidates or advertised routes that belong to a different node ID. After registration, agents sign heartbeat updates, node-removal requests, and WireGuard key-rotation requests with their node identity key. The agent rotation endpoint generates the next WireGuard keypair locally, submits the signed previous-to-next public-key transition to the control plane, persists the accepted private key in the owner-only agent state file, and updates the running state used by status, registration refreshes, and heartbeat reports. The control plane verifies signatures against the registered node identity public key, rejects missing, invalid, stale, or replayed heartbeat signatures, stores the accepted signature timestamp as the node health freshness time, only accepts endpoint candidates, advertised route updates, or path-state updates that belong to the reporting node, checks heartbeat route updates against the stored token route policy before replacing a node's route set, only removes a node when the request is signed by that node identity, and only rotates a WireGuard key when the signed previous key still matches the durable node record. This keeps health, relay-capacity, candidate, route, lease-removal, data-plane key, and pair-scoped path-state updates bound to the node that owns the registered identity.

## Control-Only Clients

The native macOS app joins the overlay through a separate control-client
protocol. A client token has role `client`, one use, no tags, no route grants,
and no relay permission. `/v1/join` rejects that role; clients can register only
through `POST /v1/clients/join`. Subsequent peer-map reads and removal use a
client-specific Ed25519 signature over the operation, client ID, Unix timestamp,
and random 192-bit nonce. Client identities cannot call node heartbeat, Signal
membership, node path, node removal, or node WireGuard-key-rotation APIs.

Clients are allocation participants but not infrastructure nodes. Their VPN IP
and public keys are durable so the overlay can route replies, while normal node
inventory, overview payloads, and node-count metrics filter them out. A separate
client metric records their allocation count.

The control plane selects one reachable gateway deterministically, preferring a
node with role `gateway` and then IPv6, public UDP, or STUN-reflexive candidates.
The client receives exactly that WireGuard peer. Its allowed IPs contain the
gateway VPN host route plus the infrastructure VPN host routes and advertised
CIDRs projected onto that gateway. The selected gateway receives clients as
direct WireGuard peers; every other node receives each client `/32` or `/128`
through the same gateway. Clients never become mesh peers, route providers,
relays, Signal members, or transit nodes.

## Control Plane HA

The control plane is designed for multiple public nodes. Durable state lives in PostgreSQL in production and SQLite in tests/dev. Ephemeral session state is separated from durable cluster state:

- Durable: node records, VPN IP leases, identity public keys, WireGuard public keys, policy, ACL, routes, relay policy, token revocation, and latest accepted path state.
- Ephemeral: active signal sessions, transient endpoint candidates, probe windows, relay session counters, and short-lived health samples.

An accepted heartbeat is one durable per-node transaction: endpoint candidates, relay capability, optional route replacement, accepted health timestamp, and the reporting node's path snapshot become visible together. Timestamp monotonicity is rechecked after acquiring the per-node serialization point, using the SQLite writer lock or PostgreSQL `FOR UPDATE` row lock. Field-specific re-registration and WireGuard key-rotation writes update only their owned JSON fields, preventing a concurrent heartbeat or lifecycle mutation from replacing unrelated newer node state.

Each public Control Plane renews a bounded lease in the shared store for its Control Plane, Signal, STUN, and Relay endpoints. Every Control Plane aggregates unexpired leases deterministically and returns the active directory with registration, heartbeat, and peer-map responses. Agents atomically persist that directory, prefer it for later Control Plane and Signal requests and STUN discovery, skip an unavailable STUN DNS/socket endpoint when another one resolves, and retain the original signed token endpoints as last-resort seeds. For Internet NAT discovery, an Agent uses only directory STUN addresses that are globally routable. If the directory contains only private, link-local, documentation, or shared `100.64.0.0/10` addresses, the default Agent resolves the configurable public STUN fallback URLs instead of mixing observations from different routing domains; `--disable-public-stun-fallback` retains private-only lab behavior. Explicit `--stun-server` values remain an operator override. `ipars token create --service-directory-url` reads the operator-authenticated directory and refuses to mint a normal HA token until at least two Control Plane, Signal, and STUN endpoints are active, plus two Relay endpoints when relay use is allowed. This invariant permits a token containing only an initially reachable node to discover the cluster before that node fails, and lets a token minted from the live directory bootstrap directly after the failure.

The public application tier is active-active, not a full mesh between every edge node. Agents negotiate data-plane paths lazily and contact the ordered public service set only when joining, refreshing state, or negotiating a path. `/v1/admin/services`, `ipars_control_plane_ha_ready`, per-service endpoint gauges, and the Web UI show the current lease view. The systemd deployment binds Signal, STUN, and Relay liveness to the local Control Plane so a partially failed host stops renewing its whole-node lease. PostgreSQL remains a separate durability boundary and must be deployed as a managed or quorum-backed HA service; two public application hosts alone cannot safely provide database election quorum.

Signed heartbeat, Signal registration, and path-negotiation reports refresh the lease timestamps of locally observed STUN candidates while the active WireGuard endpoint remains configured, preventing a healthy agent from disappearing solely because the endpoint-candidate TTL outlasts its one-time startup observation. A restarted Agent restores its own accepted endpoint candidates from the owner-only registered-node state before those loops start, so a pre-existing kernel WireGuard socket cannot leave Signal registration with an empty candidate set when it prevents rebinding the startup STUN socket.

Multi-server STUN probing skips unavailable endpoints while retaining successful observations from the same bound socket, and selects a responding server for filtering probes. Control Plane, Signal, and STUN replicas must use independent failure domains or network namespaces: processes sharing a failed namespace can remain scheduled while every service socket in that namespace is unreachable.

## VPN IP Allocation

The default pool is `10.250.0.0/16`. The former `100.64.0.0/10` default overlaps the CGNAT range used by Tailscale and similar host overlays, whose anti-spoofing rules correctly reject packets from that source range on `heteronetwork0`. Deployments must select a pool that does not overlap host, container, Kubernetes, cloud VPC, or other overlay routes. IP allocation is explicit and stored as durable lease state. Reclaiming a lease requires node removal, token/policy revocation, or administrative reassignment. `DELETE /v1/nodes/{node_id}` requires a fresh node-identity signature, removes the durable node record, clears the node's health sample and pair-scoped path records, and resets in-process allocation scanning so the released VPN IP can be reused if it is still inside the configured pool. Agents do not self-assign overlay IPs. On registration, the control plane reads existing node leases from the store and skips already assigned VPN IPs before allocating the next address; SQL stores also maintain a VPN IP uniqueness index as a last-resort guard for restarted or overlapping control-plane processes. If the store reports a VPN IP uniqueness collision during insert, registration refreshes the lease view and retries allocation so concurrent control-plane instances converge on the next free address instead of surfacing a transient join failure.

## Peer And Path Model

Path state is tracked per node pair, not per node. A node can be directly reachable from one peer, relay-only from another, and unreachable from a third.

Path states:

- `DIRECT_PUBLIC`
- `DIRECT_IPV6`
- `DIRECT_NAT_TRAVERSAL`
- `RELAY`
- `UNREACHABLE`

Path selection scores candidates using:

- latency
- loss
- jitter
- relay load
- policy permission
- route/relay cost
- stability

Direct paths are preferred when allowed and healthy. Relay paths remain encrypted end to end and are demoted automatically when direct paths recover. The agent applies a small relay-to-direct promotion margin to avoid flapping only while an unexpired relay credential is still usable; credentialless or expired relay state does not suppress a fresh direct candidate. Important peers, Kubernetes control-plane nodes, relays, and route providers can be pinned so lazy connection cleanup does not tear them down.

Direct selection is data-plane verified. A Signal response or completed UDP hole-punch send starts a pending probe but does not by itself establish `DIRECT_PUBLIC`, `DIRECT_IPV6`, or `DIRECT_NAT_TRAVERSAL`. The peer-map resolver temporarily points WireGuard at the proposed candidate while retaining an active relay session as a hot fallback. The agent takes one bounded WireGuard telemetry snapshot per Signal negotiation cycle: command and userspace backends query only `latest-handshakes`, `transfer`, and `endpoints` fields through trusted bounded `wg` subprocesses, while the kernel backend reads the same fields with generic netlink `GetDevice`. A candidate is confirmed only when the observed WireGuard endpoint matches it and either a handshake occurred after the switch or RX/TX counters increased from the pre-switch baseline. Expired probes clear the temporary endpoint preference and select an admissible relay or `UNREACHABLE`; already-direct paths must continue to show a recent handshake before they are accepted without a new probe.

Peer-map application is serialized per Agent. After, and only after, a fresh peer map is fetched successfully through the identity-signed query, command/userspace backends make one bounded `wg show <interface> peers` inventory query and the kernel backend performs a bounded generic-netlink device query. Duplicate public keys among active/pinned Node IDs and a remote peer that reuses the local interface key are rejected before inventory or peer mutation. Keys that are not in the desired set are removed directly by public key before desired peers are upserted. This removes an old key during rotation and peers that survived an Agent restart even though the process-local Node ID/key map was lost. A failed or unavailable control-plane fetch never starts inventory reconciliation, so existing WireGuard state continues to carry the data plane during the outage; an inventory failure also stops that application attempt before peer mutation and is retried by the polling loop.

The peer-map application computes the complete desired main-table route set before mutation and validates it as one plan. Peer routes carry numeric Linux route protocol `240`; the command backend obtains bounded numeric JSON inventories for both address families with `ip -N -j -details`, while the kernel backend filters IPv4/IPv6 rtnetlink dumps by table, output interface, route shape, and protocol. Unknown protocol-240 routes, old metrics, and direct `boot`/`static` routes left by earlier HeteroNetwork versions are removed before peer mutation; unrelated protocols, indirect routes, kernel-connected routes, and non-main tables are excluded from that peer ownership boundary. Docker and Kubernetes route plans use separate protocols `241` and `242`, route table `10064`, and policy-rule priorities `10064` and `10050`; their reconciliation inventories all IPv4/IPv6 tables and policy rules, removes stale owner-marked entries after restart, and migrates shape-compatible unmarked legacy entries at those fixed priorities. Inventory is fetched only after the signed peer map succeeds for peer routes, and malformed managed inventory or any query/removal failure aborts the attempt before dataplane mutation. Thus a control-plane outage preserves existing routes, while restarted agents and runtime loops converge from live kernel state without process-local route caches.

Active paths are then measured autonomously over the WireGuard interface. Each
agent runs a fixed-width UDP challenge/response service bound to its VPN IP and
periodically probes only lazy-connect-active or pinned peers with bounded
concurrency. RTT samples produce latency, exact sample-derived loss, jitter,
and path-fingerprint-scoped stability. The agent caches pair-scoped
observations, discards a round if the path changed while it was in flight, and
adds the latest fresh observation to the signed Signal negotiation envelope.
Signal rejects malformed or internally inconsistent observations and uses the
metrics only when the observed direct candidate or relay node exactly matches
the path it is scoring. Relay load remains Signal-authoritative and cannot be
supplied by an agent. This keeps initial candidate discovery optimistic while
subsequent negotiations and relay-to-direct hysteresis use measured data-plane
quality rather than static latency/loss/jitter defaults.

## ACL And Peer Visibility

Control-plane peer maps and relay maps are filtered through cluster ACL rules when rules are configured. An empty ACL list preserves the default allow-all peer visibility. When ACL rules exist, deny matches take precedence over allow matches, and unmatched peers/routes are hidden. Rules match source role/tag, target role/tag, protocol, and advertised route CIDR containment; route-specific rules can expose only the allowed subset of a node's advertised routes. Endpoint candidates older than `endpoint_candidate_ttl_seconds` or carrying unusable port-zero, unspecified, multicast, or IPv4 broadcast endpoints are removed from peer and relay maps before they are served, while the underlying node record remains available for the next heartbeat refresh. Path records older than `path_state_ttl_seconds` are excluded from control-plane path status and active path-state metrics, with stale totals reported separately so operators can distinguish old records from live path state.

Operators can load these rules into `iparsd control-plane` with repeated `--acl-rule '<json>'` arguments, or a semicolon-separated `HETERONETWORK_ACL_RULES` value. The JSON is the typed `AclRule` schema, so daemon configuration, control-plane policy, and API behavior all share the same role/tag/route/protocol/action model. Bearer-authenticated `GET /v1/policy` returns the active cluster policy, VPN pool, and loaded ACL rules for audit and status tooling.

## Lazy Connect

Agents do not establish a full mesh. They keep a compact peer map and only negotiate a path when:

- traffic for a peer or advertised route appears
- a pinned peer requires a standing path
- Kubernetes/API/service exposure needs an underlay path
- an operator runs an explicit path probe

ACL-visible idle peers are represented by passive WireGuard peer entries with
their overlay `/32` AllowedIP and host route, a loopback discard hold endpoint,
no keepalive, and a deterministic quarantine public key that is distinct from
the peer's advertised key. The route makes the first socket choose the correct
overlay source address, while the hold endpoint prevents that first send from
failing or leaving the host before the detector activates the peer. Because the
real peer key is absent, an incoming authenticated packet cannot make WireGuard
roam the passive entry back to an external endpoint. Passive-to-active
application removes the quarantine entry and recreates the real WireGuard peer
at the selected endpoint so a hold-endpoint handshake retry timer cannot delay
the first flow. No external handshake, periodic probe, or relay negotiation
runs while the peer is passive, so this does not turn the cluster into an active
full mesh. When local packet flow activates a peer, the Agent wakes path negotiation,
peer-map application, and heartbeat reporting. Its signed path snapshot carries the actual local packet
activity timestamp in a bounded marker. The shared HA Control Plane converts a fresh incoming marker into a
connection intent in the target Agent's heartbeat response; the target records
that as remote activity and immediately wakes its own path and peer-map loops.
Remote activity is tracked separately and is not emitted as another intent, so
reciprocal activation cannot create a feedback loop. Active paths return to
passive state after the cluster lazy-connect idle timeout, while passive entries
remain until the peer leaves the ACL-filtered peer map. The configured UDP
peer-probe port is internal control traffic and cannot activate an idle peer.
Conntrack detectors submit only the initiating/original tuple for activation;
the reply tuple of an inbound flow cannot be mistaken for local demand.
They also classify the Tailscale routing-bypass fwmark
`0x80000/0xff0000` as internal control traffic, preventing Tailscale's own UDP
path discovery from activating HeteroNetwork peers. Because a locally NATed
conntrack entry can remain unmarked even when its originating socket carries
that fwmark, the event detector queries `NETLINK_SOCK_DIAG` and applies the same
filter to matching marked UDP source ports before lazy-connect resolution.
A valid, rate-limited peer-probe request received through WireGuard from a
known peer VPN IP wakes a passive target immediately only when its explicit
wake-intent flag is set. An Agent sets that flag only while it has recent local
application activity for the peer; probes from a peer activated solely by a
remote intent remain quality-only. This prevents the two periodic probe loops
from alternately reactivating each other after the idle timeout. A remote wake
is recorded only on the passive-to-active transition and never becomes a local
connection intent. The initiator sends the wake probe after only a bounded endpoint-application wait,
rather than waiting for a handshake that requires the target to wake first;
the target allows its notified peer-map reconciler to install the return route
before sending the first probe response.

The default conntrack netlink event detector waits on the multicast socket
through Tokio `AsyncFd`, so new and updated flows do not wait for a polling
sleep. A bounded periodic conntrack dump complements the event stream for
flows that predate an Agent restart or are reused without another multicast
event. The first dump establishes a baseline; later dumps emit only newly seen
flows or original-direction packet/byte counter changes, so dormant stale
conntrack entries do not reactivate passive peers. Generated installers use a
one-second reconciliation interval while retaining event-driven delivery for
the normal first-packet path.

The agent runtime records peer activity through typed peer-activity and packet-flow APIs so packet detectors, probes, or operators can mark a peer active and optionally pin it; `ipars path activity` exposes that peer activation path for operator-driven lazy-connect probes. `POST /v1/path-probe`, exposed through `ipars path probe`, records operator or probe results as a pair-scoped `PathRecord` with the selected path state, selected candidate, relay node, recalculated `PathScore` from measured latency/loss/jitter/relay load/stability plus policy/cost, and optional path pin. Path probe metrics and selected candidates are validated before scoring, so negative latency or jitter, non-finite values, out-of-range relay-load/stability values, wrong-owner candidates, address-family mismatches, and unusable port-zero/multicast/broadcast endpoints cannot bias path selection. Score reasons include the stability contribution so operators can audit why a direct, NAT-traversal, or relay path was selected. Relay-to-direct promotion requires the direct candidate to beat the relay path by a small score margin, avoiding repeated flips when measured direct quality is only marginally better. Heartbeats publish each agent's local path-state snapshot; the control plane replaces that node's previous local paths on accepted heartbeat rather than merging forever, so idle-close and restart snapshots remove stale pair state from status and metrics, and repeats the selected-candidate ownership/address/usability checks before storing the snapshot. Packet-flow observations resolve the destination IP against the latest peer VPN IPs and peer-owned advertised routes, using peer VPN IP matches before longest-prefix route matches, and can carry optional source IP, protocol, source/destination ports, bounded ASCII-token detector metadata, detector-provided application hints, bounded payload prefixes, bounded and normalized conntrack status flags, and TCP state for audit/debug output; a missing protocol may retain independently observed sockaddr ports, while the shared validator used by agent HTTP, JSONL sidecar, and direct runtime submissions rejects TCP state on non-TCP observations, port metadata paired with a known non-TCP/UDP/SCTP protocol, zero ports, ACL wildcard `Any`, oversized, whitespace-padded, non-token detector or payload-prefix fields, oversized, duplicate, or unsorted conntrack status flags, and protocol-incompatible application hints before lazy-connect activation. Valid unicast destinations that do not resolve to an overlay peer or route remain counted as unmatched and are also exported with the `no_overlay_match` filtered reason. The agent classifies observation metadata into inferred conntrack lifecycle buckets such as opening, unreplied, assured, established, closing, and closed, and inferred application buckets such as DNS, HTTP, HTTPS, SSH, LDAP, SMB, FTP, TFTP, rsync, RDP, VNC, Kubernetes API, Kubelet, Docker API, CRI, containerd, etcd, PostgreSQL, MySQL, Redis, Memcached, Prometheus, OpenTelemetry/OTLP, gRPC, Kafka, NATS, MQTT, CoAP, AMQP, Cassandra, MongoDB, Elasticsearch, OpenSearch, Solr, Git, WireGuard, OpenVPN, TURN, SIP, IP tunnel, IPsec, GRE, and ICMP, preferring typed detector application hints, then high-confidence bounded first-party payload-prefix parsers, then port-based fallback guesses. Generic HTTP/TLS payload signatures can correct generic web-port guesses while well-known service ports such as Kubernetes API, Kubelet, and Docker API remain visible unless a more specific payload signature is present. The payload parsers cover DNS wire-format queries, HTTP/2 preface or mid-stream payload samples for OTLP/gRPC and CRI/containerd hints, Kubelet pod/stats/cAdvisor/resource metrics paths, versioned and unversioned Docker Engine management API paths, TLS ClientHello SNI/ALPN hints for control-plane, VPN, TURN relay, CoAP/CoAPS edge-control, voice signaling, Docker/Kubernetes runtime-management, database, messaging, cache, search, directory, file-transfer, file-sharing, and remote-admin endpoints, QUIC v1 long-header checks, SSH identification checks, WireGuard UDP message type/length checks on arbitrary ports, TURN relay-control method checks, CoAP UDP header checks, LDAP BER envelope checks, SMB direct-TCP/header checks, FTP/FTPS control greetings and commands, TFTP RRQ/WRQ request packets, rsync daemon greetings, Git smart HTTP/native pkt-line service requests, SIP request/status lines, RDP TPKT/X.224 setup packets, VNC/RFB version banners, PostgreSQL startup and framed frontend messages, MySQL handshake and command packets, Redis RESP command arrays, selected inline commands, and bounded scalar, bulk, RESP3 scalar, aggregate, and attribute server replies, Memcached text and binary command frames, Kafka length-prefixed request-header checks, NATS control-line subject/wildcard checks, stricter MQTT CONNECT payload checks and AMQP protocol header/frame checks, Cassandra version, flag, opcode, and body-shape checks, MongoDB opcode section/body checks, Elasticsearch transport status/variable-header checks, plus OpenSearch and Solr HTTP path/header with TLS SNI/ALPN hints, then exports those counts through JSON, Prometheus, and OTLP metrics. The daemon can poll Linux conntrack procfs tables with `--packet-flow-detector proc-net-conntrack`, dump the conntrack table through `NETLINK_NETFILTER` with `--packet-flow-detector conntrack-netlink`, subscribe to conntrack NEW/UPDATE multicast events with `--packet-flow-detector conntrack-netlink-events`, tail a sidecar exporter JSONL stream with `--packet-flow-detector ebpf-jsonl`, or load and attach tracepoint and/or cgroup socket-address eBPF object programs with `--packet-flow-detector ebpf-ringbuf`. Detector-specific path, object, attach, and pin options are rejected unless their matching detector is selected. The bundled object is built by `scripts/build-ebpf.sh`, exports the `HETERONETWORK_PACKET_FLOWS` ring buffer, and provides cgroup `connect4/6` plus UDP `sendmsg4/6` programs with kernel-derived protocol, destination, and available bound source metadata while preserving `ipars_sys_enter_connect`, `ipars_sys_enter_sendto`, and `ipars_sys_enter_sendmsg` as lower-fidelity syscall tracepoint fallbacks. Operators can provide their own object, repeated `--packet-flow-ebpf-attach PROGRAM:CATEGORY:NAME` tracepoint specs, and/or repeated `--packet-flow-ebpf-cgroup-attach PROGRAM` specs targeting `--packet-flow-ebpf-cgroup-path`. Runtime preflight checks that an existing custom conntrack procfs path and an existing JSONL event path are regular files rather than symlinks, that the selected eBPF object path is a non-empty regular file rather than a symlink, that each requested tracepoint has an `id` file under tracefs, that a requested cgroup target is a non-symlink cgroup v2 directory, and that bounded `/proc/sys/kernel/osrelease` metadata is present, parseable, and Linux 5.3 or newer before the retrying detector loop starts. Capability requirements are attachment-specific: cgroup hooks require `CAP_NET_ADMIN`, tracepoints require `CAP_PERFMON` or `CAP_SYS_ADMIN`, and object loading requires `CAP_BPF` or `CAP_SYS_ADMIN`. The in-process eBPF backend revalidates the ring-buffer map identifier, requires at least one attachment mode, enforces the cgroup path/program relationship, and bounds and deduplicates both attachment sets when building its loader config. It attaches cgroup programs without replacing existing programs by selecting explicit multi-program legacy attachment for Linux 5.3-5.6 and flagless cgroup BPF links, which are multi-program internally, for Linux 5.7 and newer. It reads the named ring-buffer map directly through Aya, bounds per-wakeup event draining with `--packet-flow-ebpf-ringbuf-max-events`, and decodes the shared ipars packet-flow ABI version 1 into the same resolver after rejecting unsupported event versions, unsupported protocol codes, unsupported TCP state codes, TCP state on non-TCP events, port metadata paired with a known non-port protocol, unsupported conntrack status bits, nonzero unused flags or reserved bytes, nonzero IPv4 address padding, and dropping unspecified, loopback, multicast, broadcast, and link-local destinations; destinations that pass those checks but do not resolve to peer VPN IPs or peer-owned advertised routes are exported as `no_overlay_match`. Procfs conntrack polling uses bounded reads, per-line limits, and a maximum parsed flow count configured with `--packet-flow-procfs-max-bytes`, `--packet-flow-procfs-max-line-bytes`, and `--packet-flow-procfs-max-flows` so an unexpectedly large table does not exhaust the agent; conntrack netlink dump and event reads also cap parsed flows per read with `--packet-flow-netlink-max-flows` and preserve `CTA_STATUS`/TCP protocol-info lifecycle metadata. JSONL sidecar events share the agent packet-flow schema and may include `application` hints or `payload_prefix` samples, allowing payload-aware sidecars to feed typed application classification while the resolver still matches only destination IPs and routes. Detector loops suppress duplicate flow observations for the configured `--packet-flow-dedup-ttl-seconds` window so repeated table polling does not inflate activity or metrics, export source-labelled suppression counters, and keep conntrack status or TCP state transitions as distinct fingerprints. Signal path negotiation operates only on active peers, policy-pinned peers, relay-capable peers, and route providers. Peer-map application also installs ACL-visible idle peers as passive WireGuard receive entries. The default policy pins the `control-plane` role and `route-provider` / `kubernetes-control-plane` tags, while observed peer-owned advertised routes and admissible relay capability also pin the owning peer. Idle unpinned peers replace the active endpoint with the local hold endpoint, retain only the peer host route, close relay-forwarder state after a configurable timeout, and return to passive WireGuard state; pinned peers stay connected by role, tag, route ownership, relay capability, path pinning, or explicit activity pinning.

The cgroup eBPF path also accepts bounded `--packet-flow-ebpf-sockops-attach PROGRAM` entries. The bundled `ipars_cgroup_sockops` program preserves callback flags set by other programs, enables TCP state callbacks, maps kernel TCP states into the shared ABI lifecycle model, and emits source/destination endpoint metadata for IPv4 and IPv6. Socket-address and sockops program names are validated and deduplicated separately, cannot overlap, and share the same cgroup lifetime and non-replacing legacy/BPF-link attachment selection.

Packet-flow source metadata is validated separately from destination filtering: explicitly supplied unspecified, loopback, multicast, broadcast, or link-local source addresses are treated as inconsistent transport metadata before classification or lazy-connect activation. eBPF ring-buffer events normalize all-zero source storage to an unknown source so syscall events that only know the destination do not report a spoofed unspecified source.
Memcached binary packet-flow parsing validates response magic, opcode/status pairs, extras/key/value body shape, partial-frame bounds, and error-response bodies so binary-cache traffic on non-standard ports does not rely only on a broad magic/opcode prefix.
MQTT packet-flow parsing covers CONNECT/CONNACK plus bounded PUBLISH, SUBSCRIBE, and UNSUBSCRIBE frames with topic, wildcard, shared-subscription group/filter structure, packet-ID, remaining-length, subscription-option, and MQTT v5 SUBSCRIBE/UNSUBSCRIBE property-section checks including single Subscription Identifier enforcement, avoiding short ambiguous ACK-only packet prefixes on non-standard ports.
AMQP packet-flow parsing validates protocol headers, method frame class/channel pairing, and content-header frames with basic class, zero weight, terminated property flags, and bounded property value lengths before classifying non-standard-port broker traffic, while standalone content-body frames remain unknown because the body bytes are arbitrary without preceding frame context.
Cassandra packet-flow parsing validates RESULT response kinds and their body prefixes, including rows metadata, row values, bounded nested/custom type metadata, set-keyspace strings, prepared metadata, and schema-change string sequences, so non-standard-port database traffic no longer classifies from a bare four-byte result kind alone.
DNS classification includes the standard encrypted DNS transport port 853 for TCP and UDP, so DNS-over-TLS and DNS-over-QUIC observations do not fall back to generic HTTPS/QUIC classification.
HeteroNetwork control-plane, signal, agent API, relay, STUN, and TURN traffic have dedicated packet-flow application buckets. HTTP API paths classify high-confidence join, heartbeat, peer-map/path, signal negotiation, agent activity/probe, and relay admission requests; default private service ports classify otherwise opaque control-plane, signal, agent, relay HTTP, STUN, and TURN observations; TLS SNI/ALPN hints classify externally terminated deployments; relay frame magic plus STUN magic-cookie and TURN method payloads classify relay dataplane, relay-control, and NAT probe traffic even when they share common UDP ports.
OpenVPN packet-flow classification recognizes bounded plaintext control-channel reset, control, and ACK packet prefixes for UDP and TCP transports, including TCP length framing, so non-standard-port VPN handshakes do not depend only on port 1194 or TLS SNI/ALPN hints.
Payload-prefix classification also recognizes bounded NFS ONC RPC call frames and Syslog RFC3164/RFC5424 messages, so first-packet classification can identify file-transfer, file-sharing, and logging traffic before falling back to service-port hints.

Packet-flow detector read-limit structs revalidate positive and maximum bounds when detector tasks build their procfs, conntrack-netlink, eBPF JSONL, or eBPF ring-buffer configs, so internal callers cannot bypass the CLI/runtime preflight caps.

eBPF tracepoint preflight inspects tracepoint `id` entries without following symlinks, rejecting symlinked, non-file, empty, oversized, non-numeric, or zero IDs under tracefs before loading an in-process ring-buffer detector.

Packet-flow duplicate suppression TTL uses `0` to disable suppression, caps nonzero `--packet-flow-dedup-ttl-seconds` values at 24 hours, and caps the retained fingerprint table at 1,048,576 entries so detector fingerprints cannot be retained or accumulated indefinitely.

During peer-map application, the agent revalidates selected path candidates and peer-map fallback candidates for peer ownership, direct endpoint kind, address-family consistency, and endpoint usability before passing them to WireGuard.

Elasticsearch transport frames are also recognized from bounded TCP payload prefixes using the `ES` frame marker and transport header shape, so internal cluster/search traffic does not rely only on port guesses or HTTP API paths.
OpenSearch HTTP traffic is classified from bounded `_plugins` and `_opendistro` API paths, `X-OpenSearch-*` response headers, and TLS SNI/ALPN hints, giving OpenSearch deployments a separate application metric instead of only sharing the Elasticsearch-compatible fallback.
Solr HTTP traffic is classified from bounded `/solr`, `/api/collections`, and `/api/cores` paths, `X-Solr-*` response headers, TLS SNI/ALPN hints, and TCP 8983/8984 service-port hints.
Git smart protocol traffic is classified from bounded smart HTTP `.git/info/refs?service=git-*` and `.git/git-*` RPC paths, native `git://` pkt-line service requests, TLS SNI/ALPN hints, and TCP 9418 service-port hints.

TLS ClientHello SNI names are parsed when they fit in the bounded payload prefix, allowing Kubernetes API, etcd, Prometheus, OpenTelemetry/OTLP, Elasticsearch, OpenSearch, Solr, and Git endpoints to be classified ahead of the generic HTTPS fallback.

Packet-flow runtime ingestion treats inconsistent or unbounded transport metadata as a filtered observation with the `inconsistent_transport_metadata` reason before it can activate a peer, pin a path, or increment lifecycle/application classification metrics. This keeps direct API and detector-call paths aligned with the stricter HTTP and JSONL input validation, including non-empty control-character-free detector labels, detector/payload-prefix/conntrack-status bounds, conntrack normalization, and zero-port rejection.

## NAT Traversal

The default path discovery order is:

1. IPv6 direct candidate, if policy allows and both nodes have usable IPv6.
2. Public UDP endpoint candidate.
3. STUN reflexive endpoint with signal-coordinated UDP hole punching.
4. Relay fallback.
5. `UNREACHABLE`.

STUN reflexive endpoint detection uses RFC 5389 Binding requests and success responses with `XOR-MAPPED-ADDRESS`. Agents can reuse one UDP socket across multiple STUN endpoints to classify NAT mapping behavior as no NAT, endpoint-independent, address-dependent, address-and-port-dependent, or unknown. `NoNat` is not by itself evidence of a public node: only a matching globally routable local/reflexive address becomes connectivity state `public` and candidate kind `PublicUdp`. RFC1918, loopback, link-local, documentation, benchmarking, IPv6 special-purpose, and shared CGNAT/Tailscale addresses remain `private` and never become public candidates. Agent runtime STUN operations are serialized; each successful probe or classification replaces the previous STUN-sourced candidate set, preserves candidates from other sources, and collapses repeated kind/address observations to the newest candidate so repeated API operations cannot accumulate stale duplicates. A successful probe also retains the concrete local socket address as a `LocalUdp` candidate. When a peer's reflexive address is private and both nodes advertise local addresses on the same directly routable subnet, the agent selects those local WireGuard endpoints; public reflexive endpoints remain preferred for ordinary NAT traversal. For real peer-map runtimes, startup binds that initial STUN socket to the configured nonzero WireGuard listen port, completes probing before configuring the interface, then configures WireGuard to listen on the same port. This preserves the local-port relationship required by port-preserving NATs; it does not make symmetric or address/port-dependent mappings directly reachable, which remain relay-fallback cases. Same-port, different-address STUN observations that change the reflexive endpoint are classified as address-dependent, and signal keeps that case eligible for coordinated hole punching when filtering evidence supports it. STUN probes also understand RFC 5780 `CHANGE-REQUEST`, `RESPONSE-ORIGIN`, and `OTHER-ADDRESS` attributes so compatible STUN deployments can classify NAT filtering behavior as endpoint-independent, address-dependent, address-and-port-dependent, or unknown; public STUN services can expose that behavior directly with `iparsd stun --alternate-listen`. Reproducible topology validation across network namespaces builds on these probes, including gated one-sided public-peer SNAT topologies with fixed, port-preserving, and address/port-dependent public mappings, two-sided endpoint-independent SNAT topologies for signal-coordinated UDP punching with IP-only, fixed public-port, and mixed port-preserving/fixed-port mappings, a signal-registry-generated hole-punch plan executed across a two-sided fixed-port SNAT topology, plus address/port-dependent non-traversal cases where peer-destination SNAT ports differ from the advertised STUN reflexive endpoints.

Agents include their latest NAT classification when registering with signal and when requesting path negotiation. Registration is sent to all configured signal endpoints, while path negotiation uses ordered failover and fetches hole-punch plans from the signal endpoint that accepted the negotiation. The signal service uses that classification to avoid coordinated hole punching when either side reports relay-preferred or insufficient NAT data, and filters endpoint candidates older than `endpoint_candidate_ttl_seconds` or carrying unusable port-zero, unspecified, multicast, or IPv4 broadcast endpoints before direct, IPv6, NAT traversal, or hole-punch-plan selection. `PublicUdp` candidates additionally require a globally routable address at Agent, Control Plane, and Signal validation boundaries; clients cannot relabel a private or CGNAT socket as public. IPv6 direct candidates must carry an IPv6 socket address and are rejected at control-plane registration, heartbeat update, signal upsert, and signal negotiation boundaries when the kind/address family is inconsistent. When no fresh usable direct candidate path is available, signal falls back to relay candidates when policy and capacity allow. If an agent receives `DIRECT_NAT_TRAVERSAL` but cannot fetch or execute the accepted signal endpoint's hole-punch plan, it downgrades the pair-scoped path to relay, records the direct-setup failure in the path score, and runs relay admission when the same signal response included usable relay candidates. The signal service only coordinates endpoint candidate exchange and timing. It does not forward data-plane payload.

## Relay Design

Public nodes are relay candidates only when policy, health, and capacity permit it. The control plane includes a node in relay maps and relay-candidate metrics only after it has a healthy heartbeat newer than `relay_health_ttl_seconds` in the active cluster policy and that heartbeat reports current relay capability; a heartbeat that omits relay capability clears stale relay candidacy. Signal node upserts refresh relay capability from the same local relay status source, and omit it when that status is missing or non-healthy so stale signal relay candidates are cleared. The signal registry also normalizes upserted relay capability at its boundary and stores it only when it can admit E2E relay sessions with policy enabled, public endpoint and admission URL present, available capacity, positive bandwidth, and no unusable numeric admission endpoint such as port-zero, unspecified, multicast, or IPv4 broadcast hosts. The signal registry applies the same fresh-healthy gate before offering relay candidates during path negotiation. `iparsd control-plane --relay-health-ttl-seconds` controls the control-plane window, while `iparsd signal --relay-health-ttl-seconds` controls the signal negotiation window. Control-plane and signal both report stale endpoint candidate counts and their active `endpoint_candidate_ttl_seconds` windows through JSON, Prometheus, and OTLP metrics. Relay admission checks include:

- explicit relay permission in policy
- public UDP endpoint availability
- advertised capacity
- current session/load limits
- abuse/rate-limit status
- health status

When a relay admission URL fronts a shared Service, the HTTP response identifies the relay instance that actually admitted the session. Agents accept that identity only when it is present in the current signed relay advertisements, including the local or peer relay advertisement when the endpoint is shared with a path participant, and use that advertisement's UDP endpoint for the forwarder; an unknown response identity remains an admission failure. Repeated admission for an active node pair is idempotent at the relay: it refreshes the observed endpoint addresses and expiry and returns the existing credential without allocating replacement session state, so both peers can converge on one bidirectional session when their negotiation loops race.

Relay traffic is opaque WireGuard-encrypted UDP payload. Relays route by an outer relay frame containing session metadata and an expiring bearer credential, enforce bounded session metadata and payload sizes before forwarding, enforce per-session throughput windows, strip the relay frame before forwarding, and never receive keys that can decrypt tenant payload. Oversized relay frames are counted separately in dataplane drop-reason metrics.

Relay candidates advertise both a public UDP relay endpoint and an HTTP admission URL. The `iparsd relay` daemon requires explicit `--public-endpoint` and `--admission-url` values and validates the public endpoint plus any numeric admission URL host as usable before serving, so relay status cannot fall back to bind addresses such as `0.0.0.0`. When signal negotiation selects `RELAY`, the agent selects local and peer session endpoints only from usable non-relay UDP candidates, ignoring port-zero, unspecified, multicast, and IPv4 broadcast addresses before sending admission, ranks admissible relay candidates by utilization ratio, remaining capacity, and bandwidth, attempts admission in that order, fails over to the next candidate when an admission endpoint is unavailable, rejects the session, or returns an inconsistent admission response, validates the returned node pair, session ID, endpoint echo, expiry, and frame-encodable credential before creating transient relay session state, aligns the stored pair path to the relay that actually admitted the session, and keeps the returned credential in transient runtime state rather than reporting it back through control-plane heartbeat. If signal chooses `DIRECT_NAT_TRAVERSAL` but the hole-punch plan cannot be fetched or executed, the agent falls back to the same relay admission path when usable relay candidates were offered; without a relay fallback candidate, it stores `UNREACHABLE` instead of reporting an unprepared direct path. Agent-side admission failures are also counted by observed reason, distinguishing missing local/peer endpoints, invalid relay capability, unavailable admission HTTP, rejected admission, and invalid relay responses across JSON, Prometheus, and OTLP metrics. If admission renewal fails while an existing unexpired relay session is present, the agent keeps reporting that admitted relay until expiry; if no usable relay credential remains, the pair path is downgraded to `UNREACHABLE` instead of reporting credentialless relay reachability. Relay admission refuses self-relay, unusable UDP endpoints, and same-UDP-endpoint pairs. Repeated admission for an active node pair refreshes the observed endpoint addresses and expiry and returns the existing credential without allocating replacement session state, allowing both peers to converge on one bidirectional session. Relays can also enforce a per-participating-node active session cap through `--max-sessions-per-node`; exceeded caps return `node_session_limit_exceeded` admission failures before session allocation and are exposed through relay status, Prometheus, and OTLP metrics. Relays can additionally enforce a global admission burst window through `--admission-rate-limit` and `--admission-rate-limit-window-seconds`; all session-admission attempts, including missing or rejected Bearer credentials, consume the window and exceeded windows return `rate_limited` admission failures before session allocation. Operators can configure `iparsd relay --admission-bearer-token-path` to require an HTTP Bearer token for `/v1/sessions`; agents configured with the matching `--relay-admission-bearer-token-path` read the same 32-512 byte file and attach that token to relay admission requests. Inline forms remain available but conflict with the corresponding path option, while relays without either setting preserve unauthenticated private-network admission behavior.

An agent only advertises a local relay service to the control plane when it is started with an explicit relay public endpoint and relay admission URL. The control plane still marks that relay capability enabled only when the join token policy allows relay, so public UDP reachability alone does not make a node a relay candidate.

Agent heartbeats and signal registrations can refresh relay capability and capacity fields, including active session counts from an optional relay status URL. Unreachable or non-healthy relay status makes the agent omit relay capability from those reports instead of reusing configured capacity. The control plane re-checks the node's stored token policy before accepting heartbeat updates and rewrites `enabled_by_policy` itself. When a later heartbeat omits relay capability, the control plane clears the stored capability so fresh node health cannot keep an unavailable relay in maps.

The agent-side relay dataplane forwarder wraps outbound opaque WireGuard packets in the relay frame and sends them to the selected relay UDP endpoint. Its UDP loop forwards packets from the local WireGuard socket to the relay and sends stripped inbound relay payloads back toward the local WireGuard endpoint.

Agents renew relay sessions before expiry and remove relay credentials when path negotiation selects a direct or unreachable non-relay state. Expired relay sessions are purged from runtime session and forwarder state before active-session metrics, active session lookups, renewal checks, relay forwarder endpoint resolution, or active relay stability decisions are made. This keeps relay bearer credentials short-lived without forcing admission churn on every negotiation tick.

Peer-map application always resolves WireGuard endpoints from the agent's pair-scoped negotiated path state, independently of whether relay forwarding is configured. Direct paths use the selected peer candidate, unreachable paths clear the endpoint, and paths without a negotiated record fall back to the validated peer-map candidate order. Relay paths require a matching active session and use either the supervised per-peer forwarder endpoint or an explicitly configured external forwarder endpoint.

When the agent is started with relay forwarder binding enabled, the daemon supervises per-peer local UDP forwarders for active relay sessions. Peer-map application consults negotiated path state, transient relay sessions, and the runtime forwarder endpoint table before configuring kernel WireGuard peers. Relay-selected peers with active credentials use their local forwarder endpoint as the WireGuard endpoint, allowing the forwarder to wrap opaque WireGuard packets before they leave for the public relay. The forwarder accepts outbound datagrams only from the configured local WireGuard endpoint, then validates only the visible WireGuard message type and datagram length before relay/local delivery; unexpected local senders, expired relay credentials, oversized datagrams, socket send failures, and datagrams that are clearly not WireGuard are dropped and counted without decrypting or inspecting the encrypted payload. Recoverable UDP receive errors are counted without stopping the forwarder so transient socket wakeup failures do not force relay fallback tasks through supervisor crash backoff.

Relay forwarders default to the agent's `--linux-netns` placement when one is configured, or can be pinned with `--relay-forwarder-netns`. Static runtime validation rejects unusable relay-forwarder endpoints or WireGuard endpoints, requires `--relay-forwarder-wireguard-endpoint` with `--relay-forwarder-bind`, and rejects forwarder-only namespace settings when binding is disabled even if host preflight is skipped. Runtime preflight requires `CAP_SYS_ADMIN` and validates the selected `/var/run/netns` entry before starting a namespaced relay forwarder. Because the workspace forbids unsafe code, the daemon also validates that the process is already running in the requested Linux network namespace before binding the UDP socket instead of calling `setns` internally, and that runtime check reuses the same missing-entry, symlink, directory, non-`nsfs`, and current-namespace detection used by startup preflight. Supervisors enforce a maximum number of per-peer forwarders, reap dead forwarder tasks, remove stale runtime endpoints, apply restart backoff after bind/start/runtime failures, and place repeatedly failing peers into a configurable crash-loop cooldown window.

## Docker Support

Docker support targets:

- container-to-container overlay reachability
- host-to-container reachability
- container-to-remote-node reachability

The route manager works from explicit network namespace, capability, and routing intents. The design avoids treating iptables-only rewrites as the primary integration mechanism. Rootful kernel-WireGuard deployments use `NET_ADMIN` and do not consume `/dev/net/tun`; a TUN device mount is only required when the selected userspace WireGuard implementation uses one. `iparsd agent --linux-netns` runs the Linux WireGuard and route command backends through validated `ip netns exec` placement. Peer-map application can instead use `--wireguard-backend kernel-netlink` to create the WireGuard link through rtnetlink and upsert/remove peers through WireGuard generic netlink without shelling out to `wg`, in either the current namespace or the requested `--linux-netns` namespace. `--wireguard-backend userspace-boringtun` creates and owns a TUN-backed userspace interface in the current or requested namespace, configures the private key/listen port through BoringTun's standard UAPI, assigns its local VPN `/32` or `/128` through `ip address replace`, and reconciles peer inventory and telemetry without spawning an external WireGuard process. `--wireguard-backend userspace-command` skips kernel WireGuard interface creation, validates a userspace WireGuard interface with `wg show`, configures its private key, listen port, and peers through `wg set`, and assigns its local VPN `/32` or `/128` through `ip address replace`; operators can pre-create the interface or configure `--userspace-wireguard-command` and repeated `--userspace-wireguard-arg` values so the daemon starts an external implementation and waits for readiness before peer-map sync. Route/rule application can use `--route-backend kernel-netlink` to replace `ip route`/`ip rule` execution with rtnetlink for peer-map, Docker, and Kubernetes route plans in either namespace placement. The route-manager crate rejects missing, symlinked, directory, or non-`nsfs` namespace paths immediately before opening namespaced rtnetlink sockets, so daemon preflight is not the only guard before `setns`. Docker route application can be driven by explicit container namespace, host interface, and container CIDR inputs from Compose or agent flags, or by `iparsd agent --docker-discover-networks`, which queries Docker Engine bridge networks and derives IPAM subnets dynamically. Explicit container CIDRs reject unrestricted, unspecified, loopback, link-local, multicast/broadcast, non-canonical, duplicate, and overlapping route entries, and discovered Docker IPAM subnets use the same unsafe-range, canonical-prefix, duplicate, and overlap checks before route intent construction. The route-manager crate revalidates Docker route intents before dry-run, Linux command, or kernel-netlink application, preventing lower-level callers from applying unsafe, non-canonical, duplicate, or overlapping container routes through the backend APIs. Kubernetes underlay route intents are revalidated before the same backends apply them, rejecting unsafe, non-canonical, or duplicate Service/API route CIDRs while allowing specific Service/API host routes to coexist with broader Service CIDRs. When Docker host route exposure is enabled, the agent requests those container CIDRs during join so peer maps can carry Docker reachability subject to the signed token route allowlist; Docker route advertisements are sorted and use CIDR-derived stable IDs so explicit or API discovery order changes do not churn route identity. Discovery validates Docker API version, network filter syntax, discovered bridge-network names before using them in route intent namespace labels, and positive route intervals, rejects ambiguous use with explicit container CIDRs, requires discovery for `--docker-network` name/ID filters, and resolves Docker sockets from an absolute dot-component-free explicit `--docker-api-socket`, `DOCKER_HOST=unix://...`, `/var/run/docker.sock`, or rootless `$XDG_RUNTIME_DIR/docker.sock`; non-Unix, relative, or dot-component `DOCKER_HOST` values are rejected so remote or TCP daemon settings cannot be accidentally ignored in favor of a local rootful socket. `ipars docker install --rootless` disables Docker route application by default and, without `--rootless-workload-network`, rejects Docker route/discovery flags because rootless Docker cannot provide the host route-provider boundary. With that explicit shared namespace boundary, it supports explicit CIDRs or Docker API discovery through the rootless read-only socket override; the default rootless plan adds the BoringTun dataplane override and requires user-namespace `CAP_NET_ADMIN` plus `/dev/net/tun`, while explicit `--agent-runtime-backend dry-run` remains validation-only. Arbitrary rootless Compose network lifecycle and host-to-container return-path integration still need a separate route-provider design.

Generic `RoutePlan` application also validates Linux interface names, canonical safe route CIDRs, duplicate route CIDRs, and policy-rule table/priority/address-family before dry-run, Linux command, or kernel-netlink backends run, so intent-specific Docker/Kubernetes checks are not the only guardrail on backend APIs.

The Compose bundle runs PostgreSQL, control plane, signal, relay, STUN, and an agent. It gates control-plane startup on PostgreSQL readiness, includes HTTP healthchecks for control-plane, signal, STUN, relay, and agent services, mounts distinct file-backed Control Plane operator, Agent join-token, and Agent management credentials, passes the join token through `HETERONETWORK_AGENT_JOIN_TOKEN_PATH`, keeps the base agent service free of Docker API socket access, and runs the agent with host networking plus loopback control/signal/relay URLs so the co-located agent can reach published local services while managing host WireGuard and Docker bridge routes in rootful deployments. Docker discovery install plans add `docker/compose.docker-discovery.yaml`, which binds `HETERONETWORK_DOCKER_API_SOCKET_HOST` to `/run/heteronetwork/docker.sock` as a read-only non-creating bind mount and sets `HETERONETWORK_DOCKER_API_SOCKET=/run/heteronetwork/docker.sock` only when discovery is enabled. The shared Relay admission credential is mounted into Relay and Agent and selected through their path variables. Docker route mutation, route backend selection, route advertisement, route-loop cadence, WireGuard backend selection, optional userspace WireGuard process launch/readiness/shutdown timeout settings capped at one hour, relay daemon and agent relay-capability advertisement, relay capacity, session TTL, and admission session cap/rate-limit controls, STUN HTTP health/metrics exposure, RFC5780 alternate STUN listener exposure through `HETERONETWORK_STUN_ALTERNATE_LISTEN`, and relay forwarder endpoint/bind/WireGuard endpoint/namespace plus supervisor settings are env-driven so explicit-CIDR, discovery, command or kernel-netlink routes, kernel WireGuard, userspace WireGuard, authenticated relay-admission, and relay-forwarder deployments can be selected without contradictory defaults. `ipars docker install` emits a JSON plan with the Compose manifest path, shell-quoted validation/apply commands, explicit container CIDR route hardening for unsafe, non-canonical, duplicate, and overlapping routes, relay public endpoint/admission/status URL validation plus synchronized relay-daemon and agent-advertisement environment overrides for public endpoint, admission URL, capacity, bandwidth, session TTL, and admission rate-limit settings, relay-forwarder endpoint/bind/WireGuard endpoint/namespace/supervisor validation and environment overrides, an args-aware Docker API socket preflight command for explicit or default network-discovery sockets that must be absolute, dot-component-free, non-symlink Unix sockets before the discovery override binds them into the agent, rootful Docker route environment overrides, route backend selection, duplicate-free multi-network filters, configurable host-route advertisement, positive route-loop interval, userspace WireGuard backend selection, in-process BoringTun selection for default rootless plans, and bounded external userspace launch settings for rootful plans, comma-free Compose-encoded arguments, one-hour-capped lifecycle timeout settings, RFC5780 STUN alternate-listener environment wiring, default explicit Compose CIDR settings when rootful discovery is disabled, validated Linux interface/namespace/socket names, rootless rejection for Docker route/discovery settings and namespaced relay-forwarder placement, host capability requirements including CAP_SYS_ADMIN and a host `/var/run/netns` bind mount when relay forwarder namespace placement is configured, Compose healthcheck/loopback/join-token-secret/discovery-only socket-bind/relay-admission-control/relay-forwarder notes, and private-network HTTP exposure notes. `ipars k8s install` emits matching shell-safe Helm commands and values for cluster control/signal/STUN endpoint overrides, peer-map sync, Service/API route discovery, namespace and selector filters, route-provider settings, route backend selection, join-token Secret wiring, DaemonSet pod scheduling/toleration/lifecycle/resource/rollout controls, and optional agent/relay Service exposure controls with Service type, port, source-range, and internal/external traffic policy validation.

Rootless Docker install plans place `docker/compose.rootless.yaml` after the base Compose file. The default `linux-command` plan adds `docker/compose.rootless-dataplane.yaml`, which requests only user-namespace-scoped `CAP_NET_ADMIN` and `/dev/net/tun` and selects the in-process BoringTun backend; the daemon preflight checks the TUN substrate before reading join credentials or mutating state. Passing `--agent-runtime-backend dry-run` omits that dataplane overlay and keeps the rootless stack non-mutating. Without `--rootless-workload-network`, Docker route/discovery and relay-forwarder settings remain rejected. With that explicit shared namespace boundary, explicit CIDRs or Docker API discovery are supported; discovery adds `docker/compose.rootless-docker-discovery.yaml`, uses `$XDG_RUNTIME_DIR/docker.sock` by default through a read-only non-creating mount, and reconciles same-network subnet changes without Docker network mutation or iptables. Workloads must use `network_mode: service:agent`; arbitrary rootless Compose network lifecycle and host-to-container return-path integration remain outside this contract.

The rootless Compose attachment helper is the operational bridge for selected
workload services that do not already use the shared namespace profile. It
generates the required `network_mode: service:<agent>` and `!reset` network
override, preserves the Agent health dependency, and moves explicit TCP/UDP
published ports to the Agent so rootlesskit can forward host service traffic
into the shared namespace. It intentionally does not promise arbitrary outer
host L3 routing or dynamic route-provider lifecycle across rootless engines.

Control Plane, Signal, STUN, and Relay HTTP observation routes use distinct file-backed operator credentials in the Compose bundle; the Agent join token and management credential remain separate again. A further Relay admission secret is mounted into Relay and Agent through their separate path variables without entering either environment. STUN's secret controls only HTTP metrics and does not alter the public UDP listener, while Relay capability status and admission retain their protocol-specific boundaries.

## Runtime Backends

The agent data-plane applier has an explicit runtime backend selector. `linux-command` is the default and applies peer maps, Docker route intents, and Kubernetes underlay intents through host `ip`/`wg` commands, optionally wrapped with validated `ip netns exec` placement. Docker and Kubernetes route loops remember the last successfully applied route plan, remove routes or policy rules that disappear from later explicit or discovery-backed plans before applying the replacement plan, and log removed route/rule counts with each successful reconciliation. Within that runtime, `--wireguard-backend kernel-netlink` replaces `wg`/WireGuard link command execution for peer-map application with rtnetlink and WireGuard generic netlink calls in the current or requested Linux network namespace, `--wireguard-backend userspace-boringtun` owns an in-process TUN-backed WireGuard device and UAPI peer inventory/telemetry in the current or requested namespace, `--wireguard-backend userspace-command` uses `wg show`/`wg set` against an external userspace WireGuard interface without creating a kernel WireGuard link, and `--route-backend kernel-netlink` replaces route and policy-rule command execution with rtnetlink for peer-map, Docker, and Kubernetes route plans in the same placement model. Namespaced rtnetlink socket creation temporarily enters the target namespace only long enough to open the netlink socket, then restores the caller namespace through a guard that also runs during unwind paths.
Kernel WireGuard, userspace WireGuard peer configuration, and kernel route backend selectors require `--runtime-backend linux-command` plus an active peer-map, managed userspace WireGuard process, Docker route, or Kubernetes route consumer as appropriate, and `--linux-netns` is rejected unless a Linux dataplane loop, managed userspace WireGuard process, or relay forwarder will actually use that namespace. When `--userspace-wireguard-command` is set, the daemon starts that bounded command only when it is a PATH-resolved bare command or an absolute path, using either the configured at-most-128 control-character-free `--userspace-wireguard-arg` values, each capped at 4 KiB, or the WireGuard interface name as the default argument, clears inherited environment variables while restoring only a fixed system `PATH` and `C` locale, waits up to the one-hour-capped `--userspace-wireguard-ready-timeout-seconds` for `wg show` readiness, supervises the child process to log unexpected exits, records starting/ready/exited/stopping/stopped/failed state in agent status JSON, Prometheus, and OpenTelemetry metrics, reports degraded/unhealthy heartbeat and signal health while the managed process is not ready, and kills/waits for it for the one-hour-capped `--userspace-wireguard-shutdown-timeout-seconds` during readiness failure or shutdown.
Before host mutation starts, the daemon always validates static runtime configuration, including Linux interface names, requested namespace names, daemon poll/route/renew intervals, command timeout/output bounds, userspace WireGuard launch settings and lifecycle timeout bounds, active runtime backend/namespace consumers, relay-forwarder capacity, and relay-forwarder endpoint/WireGuard endpoint usability, then validates every runtime command `PATH` entry as an absolute trusted directory chain and validates required runtime commands as executable regular files without symlink indirection, rejecting command files, PATH entries, direct parent directories, or ancestor directories that are group/world-writable or not owned by root or the effective user. It also checks `NETLINK_ROUTE`, `NETLINK_GENERIC`, and `NETLINK_NETFILTER` socket readiness for selected kernel-netlink and conntrack detectors, Docker API Unix socket readiness when Docker route discovery is enabled, `net.ipv4.ip_forward=1` when Docker or Kubernetes route forwarding is configured, `net.ipv6.conf.all.forwarding=1` when explicit IPv6 Docker/Kubernetes CIDRs are routed, `CAP_NET_ADMIN` when route/kernel-networking mutation or conntrack netlink is used, `CAP_NET_RAW` when kernel WireGuard peer-map dataplane application is enabled, `CAP_SYS_ADMIN` when Linux namespace placement, namespaced userspace WireGuard commands, or relay-forwarder namespace placement are requested, `CAP_BPF` plus `CAP_PERFMON` or legacy `CAP_SYS_ADMIN` when the in-process eBPF packet-flow detector loads and attaches tracepoint programs, non-empty regular eBPF object files, tracepoint IDs under tracefs, and requested data-plane or relay-forwarder `/var/run/netns` entries.
Namespace name validation rejects path-special `.`/`..` names and `-`-prefixed names before they can be joined under `/var/run/netns` or passed to `ip netns exec`. Namespace preflight rejects missing entries, symlinks, directories, and non-`nsfs` regular files, and warns when the entry resolves to the current process namespace. `--runtime-command-timeout-seconds` bounds each `ip`/`wg` child process used by command-backed WireGuard, userspace WireGuard peer configuration/readiness, peer-map route, Docker route, and Kubernetes underlay route application, including namespaced command execution, and timed-out command process groups are explicitly killed and reaped. `--runtime-command-output-max-bytes` bounds captured stdout and stderr per child process while still draining pipes so diagnostics cannot grow agent memory without limit, and command-backed children plus managed userspace WireGuard process launches run with inherited environment variables cleared plus a fixed system `PATH` and `C` locale. Static validation caps the per-stream capture limit at 1 MiB. The agent and route-manager command runners revalidate runtime bounds plus program/argv vectors at execution time, rejecting zero or over-one-hour timeouts, zero or over-1 MiB output capture limits, empty programs, NUL-containing program or argument strings, and oversized argument vectors before spawning child processes. They also resolve bare command programs through the fixed sanitized PATH, validate absolute command programs as trusted non-symlink executable files with trusted non-symlink parent directories, and rewrite `ip netns exec` inner programs to trusted absolute paths before spawn so namespace-wrapped commands do not rely on a second child PATH lookup. Operators can explicitly bypass the host command/netlink/sysctl/capability/path preflight with `--skip-runtime-preflight` for constrained bootstrap flows, while static configuration validation remains active. `dry-run` keeps the same peer-map polling, relay-aware endpoint resolution, Docker route planning, and Kubernetes underlay route planning loops active while using in-memory WireGuard state and dry-run route application; Docker API discovery sockets, conntrack netlink, relay-forwarder namespace placement, and eBPF ring-buffer packet-flow detectors still participate in runtime preflight because they read host or kernel state even when route and WireGuard mutation are dry-run. This lets operators validate control-plane, signal, route, and relay decisions on hosts that should not yet mutate kernel networking state.

## Kubernetes Support

This project implements Kubernetes node underlay VPN support and does not claim to be a CNI. The Helm chart installs:

- DaemonSet agent on each node
- RBAC for Service route discovery
- optional Service/API Server exposure helpers
- routing-peer configuration for node-to-node and node-to-service overlay routes

When Kubernetes underlay routing is enabled, the DaemonSet starts `iparsd agent` with the node name from the Downward API, a mounted join token Secret passed through `--join-token-path`, peer-map synchronization enabled by default, and either explicit Service/API CIDRs or RBAC-backed Kubernetes API discovery with least-privilege Service list RBAC. The agent follows Kubernetes Secret symlinks but requires the token path to resolve to a regular file and rejects inline or file-backed token input larger than 64 KiB before parsing. With `--kubernetes-discover-services`, the agent uses its ServiceAccount token, validated namespace filters, and a bounded non-control-character Service label selector to list Services and derive cluster-IP host routes; namespace and selector inputs are not accepted without discovery. It can also derive the in-cluster API server host route from `KUBERNETES_SERVICE_HOST`. Explicit and discovered Service/API route CIDRs are validated before join-time route advertisement and backend application, rejecting unrestricted, unspecified, loopback, link-local, multicast/broadcast, non-canonical, and duplicate routes while allowing specific Service/API host routes inside broader Service CIDRs. The resolved Service/API routes are requested during join and gated by the signed token route allowlist so peer maps can advertise the routing peer; route advertisements are sorted and use CIDR-derived stable IDs so explicit or API discovery order changes do not churn route identity. The Helm chart rejects direct `serviceExposure.apiServerCidrs` and `serviceExposure.serviceCidrs` values that are malformed, unrestricted, unsafe IPv4 route ranges, known unsafe IPv6 ranges, non-canonical IPv4/IPv6 CIDR routes, or exact duplicate route CIDRs before rendering, and rejects legacy `serviceExposure.apiServer` values in favor of `serviceExposure.discoverApiServer` plus explicit `serviceExposure.apiServerCidrs`; the agent and route-manager retain the full runtime CIDR checks for every rendered or discovered route. The Helm chart only renders namespace and selector arguments when API discovery is enabled and fails template rendering for invalid route intervals, invalid namespace DNS labels, repeated namespaces, oversized selectors, selector control characters, invalid chart release/namespace/name override metadata, invalid `agent.routeBackend` values, `agent.routeBackend=kernel-netlink` without peer-map or Kubernetes underlay route application, non-positive enabled peer-map poll intervals, and ServiceAccount token automount disablement when Service discovery needs Kubernetes API authentication. It renders Service discovery RBAC only when discovery is enabled, limits rules to Services, and emits namespace-scoped Role/RoleBinding objects when namespace filters are configured. The chart and install plan can also set private registry image repository/tag/pull policy overrides, image pull Secret names, peer-map sync cadence, route backend selection for command or kernel-netlink route application, agent hostNetwork and DNS policy controls, securityContext capability/privilege/read-only-root/seccomp and PodSecurityContext controls, persistent state hostPath, configurable HTTP liveness/readiness/startup probes, ServiceAccount creation, name, annotations, token automounting, validated agent pod labels, pod annotations, priority class, scheduler/runtime class, node selectors, node affinity, pod affinity/anti-affinity, tolerations, topology spread constraints, termination grace period, CPU/memory resource requests/limits, DaemonSet rollout controls, optional relay admission bearer token SecretKeyRef injection, relay forwarder endpoint/bind/namespace controls, SYS_ADMIN and host netns mount prerequisites, supervisor controls, and an optional PodDisruptionBudget. PodSecurityContext validation covers runAsUser/runAsGroup/fsGroup int64 bounds, fsGroupChangePolicy coupling, duplicate supplemental groups, and runAsNonRoot/runAsUser conflicts before rendering. The chart prepares the agent state hostPath with an initContainer that creates the mounted state directory and sets it to mode `0700`, matching the agent's owner-only state directory validation before private keys are written. Toleration validation covers label-key taint keys, `Exists`/`Equal`, standard taint effects, value omission for `Exists`, and `tolerationSeconds` only with `NoExecute`. Node affinity validation covers required and preferred node selector expressions, operator/value compatibility, and preferred weights. Pod affinity and anti-affinity validation covers required and preferred pod selector terms, namespaces, selector operator/value compatibility, topology keys, and preferred weights. Scheduler and runtime class values are validated as Kubernetes DNS subdomains before rendering. Topology spread validation pins selectors to the rendered agent labels and checks topology keys, positive int32 max skew, `DoNotSchedule`/`ScheduleAnyway`, optional positive min domains only with `DoNotSchedule`, and optional node affinity/taints policy values before rendering. Termination grace period validation accepts non-negative int64 seconds so operators can grant the agent enough time to stop relay/WireGuard dataplane loops cleanly during node drain or chart upgrades. Rollout validation covers `RollingUpdate`/`OnDelete`, integer-or-percent `maxUnavailable` and `maxSurge`, non-negative `minReadySeconds` and `revisionHistoryLimit`, and rejects `maxUnavailable=0` unless `maxSurge` is non-zero. PodDisruptionBudget rendering is opt-in, requires exactly one integer-or-percent `minAvailable` or `maxUnavailable`, and selects the same agent pods as the DaemonSet so voluntary disruptions such as node drains can be bounded separately from rollout strategy. This gives operators external ServiceAccount and workload-identity integration, service-account-token minimization, stable placement, tainted node access, zone-aware placement, lifecycle, capacity, authenticated relay admission, and upgrade availability controls without changing the DaemonSet template. The agent periodically applies the peer map and resolved routes through the selected Linux route backend, optionally through `ip netns exec`, using either an explicit route-provider node ID or the local node identity.

For a Kubernetes Service/API route provider, `hostNetwork=true` keeps the WireGuard interface in the node network namespace so kube-proxy can return forwarded responses to the overlay; the kind live route-provider gate uses this supported node-underlay mode. With `hostNetwork=false`, the chart requests namespaced `net.ipv4.ip_forward=1` and `net.ipv6.conf.all.forwarding=1` Pod sysctls, but a generic CNI still needs an explicit host-side return route or equivalent NAT integration for cross-node Service/API forwarding. The cluster kubelet must allow those unsafe sysctls. With `hostNetwork=true`, the operator must enable the corresponding node sysctls; the chart does not mutate the host network namespace.

Route ownership is explicit. `agent.routeProvider=true` makes the local agent advertise its resolved Service/API CIDRs; a remote routing peer instead requires `agent.routeProvider=false` and `serviceExposure.routeProviderNodeId`. An agent never advertises routes on behalf of that remote node. During peer-map reconciliation, locally advertised CIDRs are excluded, one connectable provider is selected for each remaining CIDR by lowest metric and then stable Node ID/route ID ordering, and that CIDR is installed in both the selected peer's WireGuard `AllowedIPs` and the Linux route table. Provider loss moves both pieces of state to the next eligible provider in one reconciliation. Kernel WireGuard is created as a kernel network link, so the Kubernetes chart does not mount `/dev/net/tun`; capabilities and kernel WireGuard support remain required.

Helm can optionally pass relay advertisement settings to the agent for public nodes that run a colocated relay service. Those settings remain inactive unless `agent.relayAdvertisement.enabled` is set and the join token policy permits relay. When both relay advertisement and the relay Service are enabled, the chart starts an `iparsd relay` sidecar in the Agent Pod and exposes its UDP/HTTP listeners through the relay Service; the sidecar's UDP target defaults to `51830` because the Agent uses `51820` for its WireGuard/STUN listener in the same Pod network namespace. The relay Service still advertises the public UDP port `51820` and HTTP status/admission port `9580` by default, while the sidecar has independent health/readiness probes, a read-only root filesystem, and dropped Linux capabilities. The sidecar resolves `--relay-node-id-path` from the shared `agent.json` state file and waits for a valid state, binding relay status and admission responses to the same identity that the Agent advertises. The Agent also rejects an admission response whose relay Node ID differs from the selected relay candidate. When relay advertisement is enabled, the chart and agent startup validation require a usable literal IPv4 host:port or [IPv6]:port public UDP endpoint, absolute HTTP(S) relay admission/status URLs with hosts, and positive capacity values before rendering or advertising capability; Kubernetes install/chart validation also rejects relay advertisement loopback and link-local numeric public endpoints or admission/status URL hosts, and the chart rejects URL userinfo, invalid explicit ports, and other unusable numeric URL hosts before rendering. Control-plane registration/heartbeat handling rejects malformed relay capability, and shared control-plane/signal relay-candidate checks apply the same public endpoint and admission URL requirements before offering a relay. If selected relay admission endpoints require Bearer auth, `agent.relayAdmissionBearerTokenSecret.name` and `.key` inject the token through `HETERONETWORK_AGENT_RELAY_ADMISSION_BEARER_TOKEN`; the chart validates that the Secret reference is complete and Kubernetes-safe before rendering.

The chart can also render optional Services for the agent HTTP API and colocated relay UDP/HTTP endpoints. `ipars k8s install` can override the agent API Service/target ports and relay UDP/HTTP Service/target ports when listeners use non-default ports. These are disabled by default so private underlay deployments do not publish node APIs unless an operator explicitly selects a Service type and annotations. The chart and install plan accept only `ClusterIP`, `NodePort`, and `LoadBalancer` Service types and only `Local` or `Cluster` internal/external traffic policies. NodePort and LoadBalancer Service types additionally require an exposure acknowledgement value in the chart; `ipars k8s install` only sets that acknowledgement when `--allow-public-service-exposure` is supplied. Explicit NodePort values are accepted for the agent API and relay UDP/HTTP ports, must be in the Kubernetes default 30000-32767 range, require a NodePort or LoadBalancer Service type, and every explicit Service NodePort or LoadBalancer health check NodePort must be cluster-unique across the agent and relay Services. LoadBalancer Services require source ranges for agent and relay exposure unless the operator also sets the explicit unrestricted LoadBalancer acknowledgement, validate those ranges as non-repeated CIDRs, require source ranges to be used only on LoadBalancer Services, reject combining source ranges with unrestricted LoadBalancer acknowledgement, optional LoadBalancer class values must be Kubernetes qualified names and only apply to LoadBalancer Services, explicit health check NodePorts only apply to LoadBalancer Services using `externalTrafficPolicy=Local` and must not reuse Service NodePorts, and disabling LoadBalancer node-port allocation only applies to LoadBalancer Services without explicit Service NodePorts. Service IP family controls support `SingleStack`, `PreferDualStack`, and `RequireDualStack` with explicit `IPv4`/`IPv6` families, requiring both families for `RequireDualStack` and rejecting duplicate or policy-inconsistent family lists. `internalTrafficPolicy` can be set to `Cluster` or `Local` for agent and relay Services when node-local Service routing is required. `sessionAffinity` can be set to `None` or `ClientIP`; the optional affinity timeout is only accepted with `ClientIP` and must be an integer between 1 second and 86400 seconds. Optional ingress-only `NetworkPolicy` resources can be rendered for the agent API and relay ports from explicit CIDR allowlists. Because the DaemonSet defaults to `hostNetwork`, the chart and CLI require an explicit host-network acknowledgement before rendering those policies unless host networking is disabled, and reject that acknowledgement when NetworkPolicy or host networking is disabled; actual enforcement depends on a NetworkPolicy-capable CNI and its hostNetwork behavior. External Services default to `externalTrafficPolicy=Local`; selecting `Cluster` requires an additional acknowledgement because cross-node forwarding can obscure source addresses used for audit and allowlist enforcement, and chart values reject that acknowledgement when it does not apply to an external `Cluster` traffic-policy Service. The install plan validates Helm release, namespace, join-token Secret name, and Secret key values before emitting namespace creation, join-token Secret wiring, Helm upgrade/install flags, private registry image repository/tag/pull policy/pull Secret values, agent hostNetwork/DNS policy values, securityContext capability/privilege/read-only-root/seccomp and PodSecurityContext values, persistent state hostPath values, HTTP liveness/readiness/startup probe values, ServiceAccount creation/name/annotation values, DaemonSet pod metadata/scheduling/node-affinity/pod-affinity/runtime-class/topology-spread/hostNetwork/service-account-token/lifecycle/resource/rollout/PDB values, least-privilege Service discovery RBAC values, cluster control/signal/STUN endpoint overrides, Service/API route discovery values, optional Service exposure toggles, and Service type/Service-port/targetPort/NodePort/LoadBalancer-class/LoadBalancer-health-check-node-port/LoadBalancer-node-port-allocation/IP-family/source-range/traffic-policy/session-affinity/NetworkPolicy/annotation overrides, with CLI annotation key/value validation for Kubernetes names, 262144-byte values, and generated Helm-safe values. The chart also validates release and namespace metadata, `nameOverride`/`fullnameOverride` resource-name overrides, image repository/tag/pull policy values, image pull Secret names, join-token Secret name/key values, agent WireGuard interface names, agent securityContext capability/privilege/read-only-root/seccomp and PodSecurityContext values, DNS policy values, state hostPath values, HTTP liveness/readiness/startup probe values, DaemonSet pod labels/annotations/node selectors/node affinity/pod affinity/priority class/scheduler/runtime class/topology spread/resource quantities/hostNetwork/DNS policy/rollout/PDB fields, Service annotation key/value pairs with string-only control-character-free values capped at 262144 bytes, required control/signal URLs with no userinfo, valid explicit ports, and usable numeric hosts, optional STUN literal socket endpoints, inactive Service exposure-specific values including type, port, appProtocol, and annotations, duplicate explicit NodePort allocations, LoadBalancer source-range CIDRs and Service-type placement, LoadBalancer classes, health check NodePorts, node-port allocation combinations, Service IP family settings, internal/external traffic policy settings, integer session affinity timeout settings, NetworkPolicy enablement/CIDR/hostNetwork acknowledgement scope, and relay-advertised literal public socket endpoints at render time for direct values usage. Relay exposure in that plan requires the public UDP endpoint and HTTP admission URL that should be advertised to peers.
Direct Helm values reject `agent.relayService.enabled=true` unless `agent.relayAdvertisement.enabled=true`, so the chart cannot silently accept a relay Service that would not expose an advertised relay endpoint.

Fixed Kubernetes ClusterIPs, LoadBalancer IPs, and externalIPs are rejected when they are unspecified, loopback, link-local, multicast, or broadcast addresses, and fixed IP values must match configured Service `ipFamilies` when those families are set. Duplicate fixed ClusterIPs and duplicate fixed external addresses (`loadBalancerIP` plus `externalIPs`) are rejected within and across the rendered agent/relay Services in both `ipars k8s install` and direct Helm values rendering. Service annotations that try to set provider-specific LoadBalancer source ranges, inbound CIDRs, fixed IPs, EIPs, static IPs, public IP prefixes, additional public IPs, IP-address lists, source NAT behavior, traffic distribution or load-balancing policy, PROXY protocol headers, LoadBalancer health checks, TLS/SSL listeners, port-wide exposure, backend protocols, LoadBalancer scope/implementation type or global access, firewall rules, WAF/Web ACLs, security policies, security groups, subnets, network tiers, resource groups, network endpoint groups, LoadBalancer operational attributes or TCP reset behavior, LoadBalancer DNS publication, resource names, tags, address pools, LoadBalancer mode or configuration selection, Private Link, endpoint-service publishing, or backend target selection are rejected so those settings cannot bypass the typed source-range, `loadBalancerIP`, `externalIPs`, health-check, Service port/appProtocol, traffic-policy, `loadBalancerClass`, exposure acknowledgement, NetworkPolicy, relay advertisement, DaemonSet scheduling, and plain HeteroNetwork listener controls. LoadBalancer source ranges and NetworkPolicy CIDR allowlists reject all-source, loopback, link-local, multicast, broadcast, non-canonical IPv4/IPv6, and repeated entries so operators cannot bypass the explicit unrestricted LoadBalancer acknowledgement by supplying `0.0.0.0/0`, `::/0`, or unsafe ranges as allowlist entries, and repeated allowlist entries cannot hide accidental chart value duplication. IPv4 and IPv6 NetworkPolicy allowlists are also rejected when they exceed the same Service's LoadBalancer source ranges, and when supplied for inactive NetworkPolicy sections. Direct Helm chart toggles for exposure, service discovery, RBAC, NetworkPolicy, ServiceAccount, PDB, peer-map, route-provider, relay-forwarder, probes, host networking, and security context are rejected unless they are boolean values, preventing quoted `"false"` values from being treated as enabled by template truthiness.
Service discovery and its RBAC-only namespace/selector settings are rejected when `serviceExposure.enabled=false`, so disabled Kubernetes underlay route application cannot still render Service-list permissions.
Agent API NetworkPolicy ingress rules follow the configured `agent.apiService.targetPort` value, and relay NetworkPolicy ingress rules follow the configured relay target ports, keeping CIDR allowlists aligned when operators override Service ports.

`ipars k8s install` rejects Service type, targetPort, or LoadBalancer source-range overrides for inactive agent API or relay Services before emitting Helm values, matching the chart's direct-value inactive exposure checks.
It also validates and emits Helm `nameOverride` and `fullnameOverride` through `--chart-name-override` and `--chart-fullname-override`, so operator-selected resource names use the same DNS-label constraints in direct chart rendering and CLI-generated plans.

Direct Helm values for agent and relay Service and target ports, NodePorts, LoadBalancer health-check NodePorts, session-affinity timeouts, peer-map cadence, relay-forwarder supervision limits, relay advertisement capacity, Kubernetes route intervals, probe/lifecycle/scheduling fields, and rollout/PodDisruptionBudget integer-or-percent values are validated as bounded non-negative integers or bounded integer-percentage values before template `int` conversion, preventing non-numeric strings, fractional values, oversized numeric values, or empty required numeric fields from being silently coerced during rendering.

CNI-owned pod networking remains the responsibility of the cluster CNI.

## API Schema

The shared `ipars-types` crate defines typed request and response models for:

- node registration
- join registration with signed token claims
- heartbeat/health reporting
- peer map and relay map retrieval
- route publication
- signal path negotiation
- relay admission and status

The wire protocol can be exposed as gRPC or HTTP. The schema is Rust-first and serializes cleanly to JSON for CLI diagnostics and tests.

Initial control-plane HTTP routes:

- `GET /healthz`
- `GET /metrics` (operator Bearer)
- `POST /v1/join`
- `POST /v1/heartbeat`
- `GET /v1/metrics` (operator Bearer)
- `GET /v1/policy` (operator Bearer)
- `POST /v1/peers/query`
- `POST /v1/paths/query`
- `POST /v1/tokens/revoke`
- `POST /v1/nodes/authenticate-signal-upsert`

Token revocation is an authenticated issuer operation. The request carries the cluster ID, token nonce, issuer node/key IDs, and a bounded-fresh timestamp under an Ed25519 signature; the control plane verifies it against the configured trusted issuer key ring before touching the durable token ledger.

Peer-map and path-status queries are authenticated node operations. Each request binds the requested node ID, query kind, bounded-fresh timestamp, and random nonce to the registered node identity signature. Query kinds are domain-separated, old path-parameter GET routes are not exposed, and each control-plane process keeps a bounded nonce cache so a captured request cannot be replayed against the same instance.

Control-plane policy and metric routes are a separate operator trust boundary. `iparsd control-plane` registers them only when a validated 32-512 byte printable token is supplied through `--operator-api-bearer-token` or the preferred file-backed `--operator-api-bearer-token-path`; requests use fixed-bound constant-time credential comparison. `/healthz` remains public, and node/issuer protocol routes retain their own signature-based authentication.

Initial signal HTTP routes:

- `GET /healthz` (public orchestration probe)
- `GET /metrics` (configured operator Bearer credential)
- `GET /v1/metrics` (configured operator Bearer credential)
- `PUT /v1/nodes/{node_id}` (node identity signature)
- `POST /v1/paths/negotiate` (node identity signature)
- `POST /v1/hole-punch` (node identity signature)

Signal node upserts, path negotiations, and hole-punch plan requests carry a bounded-fresh timestamp, random nonce, and Ed25519 node-identity signature. Signal checks node upserts against the authoritative control-plane registration, caches accepted nonces to reject replays, and removes memberships that exceed its configured authentication TTL. Path and hole-punch requests are admitted only while both participants have fresh authenticated memberships.

Signal metric routes form a separate operator trust boundary. They are registered only when `iparsd signal` receives a validated 32-512 byte printable token through `--operator-api-bearer-token` or the preferred file-backed `--operator-api-bearer-token-path`; missing or rejected credentials receive a Bearer challenge using fixed-bound constant-time comparison. Omitting the credential leaves both metric routes absent while `/healthz` and signed protocol routes remain available.

The STUN UDP Binding and RFC5780 alternate-listener protocol remains public so agents can discover NAT mappings and filtering behavior. Its HTTP `/healthz` route is public for orchestration, while `GET /metrics` and `GET /v1/metrics` are registered only when `iparsd stun` receives its own validated operator Bearer credential. Omitting that credential leaves HTTP metrics absent without changing UDP service availability.

Initial relay HTTP routes:

- `GET /healthz` (public orchestration probe)
- `GET /metrics` (configured operator Bearer credential)
- `GET /v1/status` (public relay capability/health protocol)
- `POST /v1/sessions` (optional admission Bearer credential plus abuse controls)

Relay Prometheus counters form a separate operator boundary and are absent when no operator credential is configured. The public status route remains available because relay-hosting Agents use it to refresh advertised health and capacity; session creation retains its distinct admission credential, rate limits, and capacity controls.

Initial agent HTTP routes:

- `GET /healthz`
- `GET /metrics`
- `GET /v1/status`
- `GET /v1/metrics`
- `GET /v1/paths`
- `GET /v1/path-events`
- `POST /v1/path-probe`
- `POST /v1/stun-probe`
- `POST /v1/nat-classification`
- `POST /v1/peer-activity`
- `POST /v1/packet-flow`

The Agent listener defaults to loopback. Non-loopback listeners require a 32-512 byte management Bearer token, and configured authentication covers `/metrics` plus all `/v1/*` reads and mutations. `/healthz` remains unauthenticated for orchestration probes. CLI Agent calls, Compose, and the Helm DaemonSet carry the same credential through file-backed or Secret-backed configuration; the management credential is intentionally separate from the signed join token.

## Observability

All daemons emit:

- structured logs
- counters/gauges/histograms suitable for metrics export
- path-change events
- relay admission/refusal events
- token validation/revocation events

Critical events include path promotion/demotion, relay fallback, relay abuse refusal, VPN IP allocation, route publication, and policy denial.

The control plane exposes JSON and Prometheus metrics for registered nodes, relay-capable nodes, last reported health, active pair-scoped path state counts, stale path counts, and path-state TTL. Signal exposes JSON and Prometheus metrics for registered nodes, relay candidates, NAT classifications, health freshness, node upsert counts, path negotiation counts, hole-punch plan counts, and NAT suppression strategy counts. Agents expose typed JSON metrics, Prometheus scrape metrics, and a bounded structured path-change event buffer. The metrics include candidate, path, relay session, relay admission attempt/success/failure counters with agent-observed failure reasons, relay forwarder, lazy-connect active/pinned/index gauges, path-probe, peer-activity and packet-flow counters, packet-flow duplicate suppression counters, packet-flow lifecycle and application classification counters, filtered destination reason counters, per-forwarder outbound/inbound packet and opaque payload byte counters, per-forwarder recoverable socket receive errors, unexpected-local-sender, expired relay credential, oversized datagram, socket send error, and non-WireGuard drop counters, retained/total/dropped path-change event counters, and per-path-state counts. Relays expose Prometheus scrape metrics for capacity, active sessions, admission attempts/successes/failures with failure reasons such as unauthorized, admission-denied, and rate-limited attempts, policy-enabled and e2e-only state, health, forwarded opaque payload bytes, UDP datagram counters, byte counters, and drop reasons. Path events record the previous and new path state, relay node, selected candidate, and score for create/change transitions.

Every `iparsd` subcommand shares root observability options. When `--otel-enabled` or `HETERONETWORK_OTEL_ENABLED=true` is set, `iparsd` configures OTLP HTTP/protobuf exporters for traces, tracing-backed logs, and metrics. `--otel-endpoint`/`HETERONETWORK_OTEL_ENDPOINT` accepts the collector base URL, with `/v1/traces`, `/v1/logs`, and `/v1/metrics` selected per signal. `--otel-service-name`/`HETERONETWORK_OTEL_SERVICE_NAME` overrides the default `iparsd-<component>` service name, and `--log-filter`/`HETERONETWORK_LOG_FILTER` controls both local formatted tracing output and exported telemetry. The control-plane daemon records node, relay-candidate, health, VPN-pool, join-token-ledger, active path, stale path, path-state TTL, and per-path-state gauges into `ipars.control_plane.*` OTLP metrics. The signal daemon records node, relay-candidate, NAT-classification, zero-filled fresh-NAT-strategy gauges, health-total, health-state, stale-health, request, and NAT-suppression strategy metrics into `ipars.signal.*` OTLP metrics. The relay daemon records active/capacity/e2e-only gauges, admission counter and failure-reason deltas, and dataplane counter deltas into `ipars.relay.*` OTLP metrics; the agent records candidate/path/relay-session gauges, per-path-state gauges, relay-admission counter and failure-reason deltas, lazy-connect active/pinned/index gauges, path-probe, peer-activity, packet-flow, packet-flow duplicate suppression, packet-flow lifecycle and application classification, filtered destination reason, and relay-forwarder counter deltas into `ipars.agent.*` OTLP metrics. `--otel-metrics-poll-interval-seconds`/`HETERONETWORK_OTEL_METRICS_POLL_INTERVAL_SECONDS` controls control-plane, signal, relay, and agent snapshot polling and must be positive.

## Security Model

- Join tokens are signed, scoped, expiring, and revocable through the token ledger by cluster ID/nonce.
- Token revocation requires a fresh signature from a configured trusted issuer key and rejects unsigned or tampered ledger mutations.
- Token policy constrains relay permission, route advertisements contained within allowed CIDRs, allowed tags, and max-use admission.
- Identity keys authenticate nodes to the control plane.
- WireGuard keys provide data-plane confidentiality.
- Agent management APIs default to loopback and require Bearer authentication for non-loopback listeners; `/healthz` is the only intentionally unauthenticated Agent route when protection is configured.
- Relays cannot decrypt payload.
- Public nodes are not automatically relays; policy, health, and capacity are required.
- Kubernetes NetworkPolicies can restrict agent API and relay ingress by CIDR when the cluster CNI enforces them; hostNetwork enforcement is explicitly acknowledged because it is implementation-dependent.
- Control-plane registration rejects relay capability advertisements unless the join token policy allows relay, and accepted capabilities are marked as enabled by policy by the control plane.
- ACLs are evaluated by tag, role, route, protocol, and relay permission, and control-plane peer maps hide peers or advertised routes that are not allowed.
- Key rotation supports overlapping issuer token-signing keys plus identity and WireGuard key families separately. WireGuard public-key updates use signed previous-key-to-next-key transitions and are rejected if the stored current key changed before the rotation is applied.

## Failure Behavior

- Control plane down: existing WireGuard data-plane paths remain active until idle timers or kernel state changes remove them.
- Signal down: existing paths remain active; agents retry registration and path negotiation against the remaining configured signal endpoints, and new NAT traversal waits for signal recovery only when all configured signal endpoints are unavailable.
- Relay down: affected pairs demote to probing/direct candidates or another relay if available; otherwise `UNREACHABLE`.
- STUN down: known candidates remain usable; new NAT classification is degraded.
- Agent restart: identity and WireGuard keys are read from disk only when the state path resolves to an owner-only regular file, with symlinked or group/world-accessible state paths rejected; state updates are written through owner-only temporary files and atomically renamed into place. The agent can re-register with a join token, refresh signal-service node state, negotiate pair paths, execute UDP hole-punch plans, report heartbeat/path state, then pinned routes and current peer map are rehydrated through explicit backend application and refreshed by continuous peer-map polling.

## Integration Tests

Required integration scenarios:

- 3-node topology: one public bootstrap/control/relay node and two NAT nodes.
- 10-node topology: mixed public, NAT, Docker, and route-provider nodes.
- NAT matrix: full cone, restricted cone, port-restricted, symmetric, and double NAT where reproducible with network namespaces.
- Relay fallback: forced direct-path failure and automatic direct promotion after recovery.
- Docker Compose: host/container/remote reachability, including generated rootless shared-namespace workload attachment and host access through published TCP/UDP ports.
- Kubernetes: DaemonSet agent route publication and node-underlay reachability.

## Scale Plan

### 3 Nodes

Target: direct path setup below 1 second on LAN-like RTT and relay fallback below 2 seconds. A single control-plane instance is acceptable for dev.

### 10 Nodes

Target: lazy connections only for active flows/pins. Peer-map updates are delta-based. Relay admission uses explicit capacity and health.

### 1000 Nodes

Target: no full mesh. Agents subscribe to relevant peer/route deltas by tag, role, and route interest. Control-plane state is partitionable by cluster ID and node ID. Relay maps are ranked by region/cost/load. Load tests must measure registration rate, peer-map fanout, signal negotiation concurrency, relay throughput, and path-state churn.

The `ipars-load` harness provides executable `three`, `ten`, and `thousand` scenarios against in-memory control-plane and signal components, plus `--transport http` for driving loopback TCP HTTP control-plane and signal endpoints with the same workload. `--transport relay-udp` drives relay HTTP admission and loopback UDP forwarding with validated configurable packet count and payload size. `--transport daemon` validates the requested agent and control-plane process counts, then spawns separate `iparsd` control-plane, signal, RFC5780 alternate-listener STUN, relay, and dry-run agent processes with inherited environment variables cleared, a fixed system `PATH` plus `C` locale restored, and distinct generated scoped operator credentials added only to Control Plane, Signal, STUN, and Relay children. A separate run-scoped relay admission credential is written to an owner-only regular file, its path is passed to Relay and every Agent while being redacted from retained argv, authenticated harness admission requests use the credential, and the file is removed after readiness. Multiple control-plane processes share one SQLite store, and signed agent join tokens include all runtime control-plane bootstrap URLs with the selected scenario's relay and route-provider policy distribution for the launched agent prefix. Scenario relay agents advertise the colocated daemon relay endpoint, admission URL, status URL, and capacity so control-plane relay-candidate accounting is exercised by the external process path, while scenario route-provider agents advertise dry-run Docker container CIDRs that match their signed route allowlists. The harness writes signed join tokens into post-write validated owner-only regular private run-time files, passes `--join-token-path` instead of exposing token material through argv, scrubs those token files after agent readiness, scrubs agent state files after child shutdown, and hardens retained regular runtime files to owner-only mode before preserving diagnostics. It captures each child process stdout/stderr log under the run-time directory, updates an owner-only atomically replaced token-redacted `run-manifest.json` throughout startup, startup-readiness, and measurement with phase, workload/options snapshot, run-start and last-update timestamps, role, PID, child running/exited state, exit status/code when liveness detects a failed child, STUN HTTP plus primary and alternate STUN UDP endpoints, log path, log byte count, log-tail SHA-256 diagnostics, and completed relay/failover dataplane measurement counters, refreshes the manifest before returning readiness failures, can retain that directory with `--daemon-keep-runtime-dir` for post-failure analysis, watches child process liveness during readiness and measurement phases, reports log tails on liveness or readiness failures, verifies signal, relay, and STUN health before agent startup, verifies agent status and endpoint-candidate availability, drives peer activity into daemon agents, waits for reachable `/v1/paths` state from their own signal negotiation loops, then waits for the shared-SQLite control-plane metrics and `/v1/paths/query` status responses to expose the heartbeat-reported path state on every control-plane endpoint, verifies agent registration and peer-map visibility across every control-plane endpoint, negotiates paths through the signal daemon, verifies relay UDP forwarding through the relay daemon, keeps the admitted relay credentials, stops one redundant control-plane process when available, and rechecks survivor peer maps plus path-state metrics, `/v1/paths/query` responses, and existing relay UDP session delivery before writing the completed manifest after stopping all remaining child processes so retained logs are final. It reports registration count/time, peer-map fanout, advertised route count, sampled active pair negotiations, relay candidate count, HTTP request counts, daemon process counts, final control-plane, signal, STUN, and relay health/metrics snapshots, per-control-plane-endpoint min/max consistency ranges, per-agent endpoint-candidate and path-state min/max ranges, failover survivor path-state/status ranges, failover relay UDP packets/bytes, relay UDP packets/bytes/throughput, relay capacity/admission counters and failure reasons, STUN alternate-listener and Binding request/response counters, retained daemon runtime and manifest paths when requested, and selected path-state totals as JSON, and the success validator rejects missing agent status coverage, missing agent endpoint candidates, missing agent runtime or control-plane path-status reachable paths, missing failover survivor control-plane path metrics or status, missing failover relay dataplane delivery, advertised-route loss, missing control-plane metrics endpoint coverage, relay-candidate min/max loss, relay session/capacity/policy/E2E/admission-counter skew, missing STUN HTTP metrics, missing STUN alternate-listener metrics, insufficient successful startup STUN probes, unexpected STUN error counters, unexpected retained runtime artifacts, retained sensitive or transient runtime residue, duplicate HTTP or UDP endpoints including STUN HTTP and primary/alternate STUN socket reuse, duplicate, empty, role-mismatched, or serial/order-skewed retained child logs, and retained manifest phase/timestamp/workload/readiness-timeout/endpoint/child-role-order/PID-presence-uniqueness/exit-status-code/process/log-diagnostic/measurement/permission/owner mismatch, including log byte-count and tail-hash mismatch against retained files, rather than treating them as informational fields. A gated `HETERONETWORK_TEST_IPARSD_BIN=...` test drives this external daemon transport with retained runtime output and validates the relay-candidate and completed-manifest contracts. The 1000-node scenario samples active pairs rather than negotiating all possible pairs so the harness exercises the lazy-connect assumption while still measuring current peer-map fanout.

The current daemon transport assigns distinct generated operator credentials to Control Plane, Signal, STUN, and Relay children through the sanitized child environment. None of those values appears in child argv or the retained manifest; authenticated readiness and final metric reads prove each credential path.

Daemon secret cleanup revalidates each tracked join-token and agent-state path against the daemon runtime directory before removal, including expected suffix, regular-file type, non-symlink metadata, and canonical runtime-local placement, so failed cleanup leaves the target in the harness diagnostic list instead of silently losing it.

Failover survivor validation also records and gates relay-candidate min/max, healthy/degraded/unhealthy node min/max, path-state min/max, and per-node path-status freshness so HA loss of relay eligibility or node health cannot pass as a peer-map-only success. The same failover phase re-queries every agent status and agent `/v1/paths` endpoint after stopping one control plane, requiring the original node identity, endpoint candidates, and reachable runtime path state to remain available, and sends one additional opaque payload on each already admitted relay UDP session to prove the existing data plane does not depend on the stopped control-plane process.

The bundled Docker Compose and Helm manifests use HTTP service URLs for control-plane, signal, relay admission, and agent APIs because `iparsd` serves plain HTTP directly. In Compose, the host-network agent reads its join token from the mounted Compose secret and connects to co-located services through loopback URLs after service healthchecks report readiness. In Kubernetes, optional agent API and relay Services expose appProtocol metadata for the HTTP agent API, relay admission HTTP port, and WireGuard relay UDP port so load balancers, service meshes, and observability tooling can classify the ports without relying only on names; fixed ClusterIPs, fixed LoadBalancer IPs, externalIPs, and `publishNotReadyAddresses` stay disabled unless explicitly enabled for environments that intentionally route through those Service behaviors. Production deployments should terminate TLS at a reverse proxy, load balancer, or Kubernetes Ingress before exposing those APIs outside the private deployment network.

## Implementation Roadmap

1. Shared typed models, CLI surface, and signed-token primitives.
2. Control-plane store trait plus in-memory, SQLite, and PostgreSQL backends.
3. Long-running control-plane, signal, STUN, relay, and agent daemons.
4. Kernel WireGuard backend: Linux `ip`/`wg` command runner and namespace-aware kernel netlink WireGuard peer backend exist; broaden privileged namespace integration coverage.
5. Route manager for Linux policy routing, Docker namespaces, and Kubernetes node underlay: `ip route`/`ip rule` command backend, namespace-aware rtnetlink backend, and validated namespace command execution exist; harden namespace lifecycle.
6. NAT traversal integration tests with network namespaces. The gated suite covers route backends, signal-generated hole-punch plans, endpoint-independent/fixed-port/mixed-port and address/port-dependent SNAT topologies, one-sided public-peer NAT, and opaque relay fallback. `scripts/agent-nat-smoke.sh` runs two real Agents through the full registration, peer-map, WireGuard, relay fallback, and encrypted-dataplane loop behind endpoint-independent, fixed-port, and mixed-port two-sided SNAT, then promotes to direct; its one-sided and one-sided-port-preserving profiles run the same loop with a public peer and an endpoint-independent SNAT Agent; its symmetric, asymmetric, and one-sided-symmetric profiles run the same loop with address-and-port-dependent classification and keep the pair on relay. Production acceptance still requires testing the deployment's real NAT and firewall behavior and expanding full-Agent coverage to additional NAT profiles.
7. Relay abuse controls, metrics, and production hardening.
