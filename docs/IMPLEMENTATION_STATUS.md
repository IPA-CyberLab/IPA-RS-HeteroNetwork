# Implementation Status

This file tracks the gap between the requested final system and the current repository state.

## Implemented In This Baseline

- Kubernetes Service exposure hardening rejects unspecified, loopback, link-local, multicast, and broadcast fixed ClusterIP, LoadBalancer IP, or externalIP values, rejects fixed Service IP values whose address family is outside configured Service `ipFamilies`, rejects duplicate fixed ClusterIPs and fixed external addresses (`loadBalancerIP` plus `externalIPs`) within and across rendered agent/relay Services, rejects unrestricted all-source, unsafe, and non-canonical IPv4 CIDRs in LoadBalancer source ranges and NetworkPolicy allowlists, rejects duplicate source-range and NetworkPolicy allowlist CIDRs, rejects NetworkPolicy allowlists that exceed the same Service's LoadBalancer source ranges, rejects Service annotations that try to configure LoadBalancer source ranges, inbound CIDRs, fixed LoadBalancer addresses, source NAT behavior, traffic distribution or load-balancing policy, unsupported PROXY protocol headers, provider-specific LoadBalancer health checks, TLS/SSL listeners, port-wide exposure, backend protocols, LoadBalancer scope/implementation type or global access, firewall rules, security groups, subnets, network tiers, resource groups, network endpoint groups, LoadBalancer operational attributes or TCP reset behavior, LoadBalancer DNS publication, resource names, tags, address pools, Private Link, endpoint-service publishing, or backend target selection outside the typed controls, rejects inactive NetworkPolicy CIDR allowlists, rejects non-boolean direct Helm chart toggles across exposure, service discovery, RBAC, NetworkPolicy, ServiceAccount, PDB, peer-map, route-provider, relay-forwarder, probes, host networking, and security context values, validates internal/external traffic policies, traffic distribution, and session affinity before rendering Services, scopes hostNetwork NetworkPolicy acknowledgement to enabled NetworkPolicies on host-networked agents, keeps NetworkPolicy ingress port rules aligned with configured target ports with CLI coverage for Service/target-port override combinations, and has `ipars k8s install` emit chart exposure acknowledgement for `ClusterIP` Services that intentionally set externalIPs.
- Kubernetes Helm chart metadata now scopes generated resource names by Helm release through the `fullname` helper and adds `app.kubernetes.io/instance` labels/selectors across the DaemonSet, Services, NetworkPolicies, PDB, ServiceAccount, and RBAC resources, preventing same-namespace release collisions and cross-release Service/NetworkPolicy/PDB selection.
- `ipars k8s install` now wires validated Helm `nameOverride` and `fullnameOverride` values through `--chart-name-override` and `--chart-fullname-override`, with chart defaults and smoke coverage for overridden resource names.
- Kubernetes DaemonSet scheduling now supports validated topology spread constraints from chart values and `ipars k8s install --agent-topology-spread`, automatically binding each constraint's selector to the rendered agent labels and rejecting invalid topology keys, max skew, min domains, unsatisfiable mode, and node policy fields before rendering.
- Kubernetes DaemonSet scheduling now supports validated node affinity from chart values and `ipars k8s install --agent-node-affinity-required`/`--agent-node-affinity-preferred`, covering required/preferred node selector expressions, operator/value compatibility, and preferred weights.
- Kubernetes DaemonSet scheduling now supports validated pod affinity and pod anti-affinity from chart values and `ipars k8s install --agent-pod-affinity-required`/`--agent-pod-affinity-preferred`/`--agent-pod-anti-affinity-required`/`--agent-pod-anti-affinity-preferred`, covering required/preferred pod selector terms, namespaces, selector operator/value compatibility, topology keys, and preferred weights.
- Kubernetes DaemonSet scheduling now supports validated custom scheduler and RuntimeClass placement from chart values and `ipars k8s install --agent-scheduler-name`/`--agent-runtime-class`, rejecting invalid Kubernetes DNS subdomain names before rendering.
- Kubernetes DaemonSet pod security now supports validated PodSecurityContext values from chart values and `ipars k8s install --agent-run-as-user`/`--agent-run-as-group`/`--agent-run-as-non-root`/`--agent-fs-group`/`--agent-fs-group-change-policy`/`--agent-supplemental-group`, including int64 bounds, duplicate supplemental group rejection, fsGroup policy coupling, and runAsNonRoot/runAsUser conflict checks.
- Kubernetes DaemonSet health checking now renders validated startup probes alongside liveness/readiness probes from chart values and `ipars k8s install --agent-startup-*`/`--disable-agent-startup-probe`, so slow agent bootstrap can be bounded separately from steady-state health and readiness.
- Kubernetes relay Service chart rendering rejects `agent.relayService.enabled=true` unless `agent.relayAdvertisement.enabled=true`, preventing direct Helm values from silently enabling a Service that would not expose an advertised relay endpoint.
- `scripts/helm-smoke.sh` runs a Dockerized pinned Helm CLI to lint and render the chart across default, agent API Service, relay Service, cluster endpoint render/validation, Service traffic policy/distribution/affinity, pod security context, pod health probes, node affinity, pod affinity/anti-affinity, scheduler/runtime class, topology spread, `externalTrafficPolicy=Cluster` acknowledgement, LoadBalancer source-range and unrestricted-exposure acknowledgement, source-range/inbound-CIDR/fixed-address/source-NAT/traffic-distribution/PROXY/health-check/listener/port-exposure/scope/global-access/firewall/placement/attribute/tcp-reset/DNS/resource/private-link/backend-target annotation rejection, duplicate fixed external-address rejection, NetworkPolicy, route-disabled, namespace-scoped Service discovery RBAC, relay-forwarder namespace, agent state path rejection, and expected-failure configurations.
- Kubernetes underlay chart validation rejects Service discovery and its RBAC-only namespace/selector settings when `serviceExposure.enabled=false`, so disabling route application cannot leave Service-list permissions rendered.
- Rust workspace with dedicated crates for types, crypto, control plane, signal, relay, STUN, agent, route manager, and CLI.
- Typed models for node, peer/path state, relay capability, token, policy, ACL, route, and health.
- Ed25519 signed join token creation and verification.
- X25519/WireGuard key generation primitive.
- Pair-scoped path scoring with direct/IPv6/NAT traversal/relay/unreachable states.
- In-memory control-plane registration and store-aware VPN IP allocation that skips already assigned addresses after durable-store restarts and retries after durable-store VPN IP insert races.
- SQLite and PostgreSQL control-plane store implementations with SQLite round-trip tests, VPN IP uniqueness indexes, and typed duplicate node-ID/VPN-IP insert errors.
- Token ledger records, control-plane revocation API, revocation state, and race-safe max-use enforcement for in-memory and SQL stores using compare-and-swap SQL token-use updates.
- Control-plane join service that validates signed join tokens, issuer keys, overlapping issuer key rotation sets, cluster/time constraints, ledger admission, CIDR-containing route policy, relay-capability policy and relay advertisement shape including unusable numeric relay admission URL hosts, Ed25519 identity-public-key format, WireGuard public-key format, identity-derived node IDs, candidate ownership, endpoint candidate kind/address-family consistency, route ownership, route ID/metric/unsafe-or-non-canonical-CIDR uniqueness shape, and node registration.
- Control-plane relay maps and relay-candidate metrics require relay capability policy, capacity, E2E-only relay mode, and a fresh healthy heartbeat inside the configured relay health TTL, and fresh heartbeats that omit relay capability clear stale relay candidacy.
- Control-plane heartbeat updates require a fresh node-identity signature, verify against the registered identity public key, reject replayed or older signed heartbeats, store the accepted signature timestamp as health freshness time, refresh node route advertisements when signed heartbeats include valid route ID/metric/unsafe-or-non-canonical-CIDR uniqueness shape, preserve existing routes when heartbeats omit route updates, clear relay capability when the reporting node no longer advertises it, reject malformed relay capability updates before persistence, and reject candidate/route/path-state updates that do not belong to the reporting node, whose advertised routes exceed the stored token route policy, whose selected path candidate belongs to the wrong peer, conflicts with its address family, mismatches the selected direct path state, or uses an unusable port-zero/unspecified/multicast/IPv4-broadcast endpoint, whose relay path carries a direct selected candidate, whose unreachable path carries a selected candidate, or whose relay path lacks an ACL-visible eligible registered third-party relay node.
- Control-plane WireGuard key rotation API requires a fresh node-identity signature over the previous and next WireGuard public keys, validates both key formats, rejects stale current-key transitions, persists the updated node record in memory/SQLite/PostgreSQL stores, and returns refreshed peer/relay maps.
- Control-plane HTTP crate with typed health, join, signed WireGuard key rotation, policy inspection, ACL-filtered and candidate-fresh peer-map, ACL-filtered and TTL-fresh path-state status, and JSON/Prometheus metrics routes with zero-filled path-state counts backed by the join service.
- Control-plane peer-map and relay-map visibility filtering through cluster ACL rules matching source/target role, tag, route CIDR containment, and protocol, with deny rules taking precedence, default allow-all behavior when no rules are configured, and stale or unusable port-zero/unspecified/multicast/IPv4-broadcast endpoint candidate removal before maps are served.
- `iparsd control-plane` daemon that serves the control-plane HTTP router with in-memory, SQLite, or PostgreSQL storage, validates path-safe cluster, issuer-node, issuer-key, and trusted-issuer key identifiers before URL/metric exposure, accepts extra trusted issuer public keys for token-signing key rotation overlap, loads repeated JSON `--acl-rule` values or semicolon-separated `IPARS_ACL_RULES`, and exposes relay health, endpoint candidate, and path-state TTL configuration.
- Signal registry, typed signal HTTP routes, JSON/Prometheus metrics, and `iparsd signal` for endpoint candidate exchange with candidate ownership and kind/address-family validation, candidate freshness TTL metrics, NAT classification TTL, minimum-confidence policy, fresh traversal-strategy metrics, fresh low-confidence NAT metrics, ACL-denied path/relay/hole-punch visibility counters, zero-filled path-negotiation state counts, relay candidate lookup with unusable numeric admission URL host filtering, source/target-registered state-counted path negotiation, and hole-punch planning with NAT-classification suppression metrics including suppressing strategy counters.
- RFC 5389 STUN Binding request/success response handling with `XOR-MAPPED-ADDRESS` decoding and bounded response timeouts, RFC 5780 `CHANGE-REQUEST`/`RESPONSE-ORIGIN`/`OTHER-ADDRESS` support exposed through optional `iparsd stun --alternate-listen`, client-side rejection of unusable STUN server sockets before probing, multi-server NAT mapping/filtering classification that treats unknown filtering evidence as insufficient for coordinated hole punching, and `iparsd stun` daemon support for public endpoint detection.
- Relay admission/status HTTP API with optional Bearer-token admission gating, path-safe participant node-ID validation with invalid-request failure counters, self-relay, unusable endpoint, same-UDP-endpoint, duplicate active node-pair, and per-participating-node active-session admission refusal, configurable relay admission burst limiting that also covers unauthorized admission attempts, Prometheus and OTLP relay capacity/per-node-cap/admission/dataplane metrics with admission attempt/success/failure counters and failure reason counters, cumulative relay dataplane counters/drop reasons including malformed empty-payload, unsafe routed-frame node metadata, and oversized relay frames, expiring credentialed opaque UDP forwarding loop with path-safe relay-node ID validation, positive and 24h-capped session TTL/admission-window validation plus checked duration conversion, bounded relay-frame metadata/payload sizes, per-session rate limits, and `iparsd relay` startup validation requiring explicit usable relay advertisement endpoints and rejecting unusable numeric admission URL hosts.
- Relay status JSON plus Prometheus and `iparsd` OTLP admission/drop reason metrics are zero-filled across every known relay failure/drop reason so dashboards do not lose time series before a reason is first observed, with HTTP Prometheus coverage explicitly checking invalid-session-credential dataplane drops.
- CLI `init` and `token create` can sign join tokens with a reusable issuer private key and key ID, including bounded regular-file issuer private-key path loading with symlink rejection, path-safe cluster/key/role/tag claim validation, 64-tag and 30-day TTL caps, typed and scheme-validated control-plane/signal/STUN/relay bootstrap endpoints with numeric-host usability rejection for port-zero, unspecified, multicast, or IPv4 broadcast socket addresses, relay permission, canonical and non-overlapping route allowlists that reject unrestricted/unsafe/duplicate CIDRs and cap `--allowed-route` at 256 entries, and max-use token policy flags; `init` validates the public endpoint and derived control-plane/signal/relay admission ports before signing the bootstrap token, emits typed `iparsd` bootstrap commands for control-plane, signal, STUN, and relay, and can optionally spawn them with inherited environment variables cleared, a fixed system `PATH` plus `C` locale, per-service logs after creating owner-only daemon state/log directories, and owner-only non-symlink, non-hardlinked log files; `join <token>` creates node identity/WireGuard keys, builds `JoinNodeRequest`, validates control-plane bootstrap or override API base URLs, and tries those endpoints in order; `token revoke` validates path-safe cluster/nonce inputs and posts typed token revocation requests to the control plane after the same API base URL validation; `status`, `peers`, `routes`, `relay status`, and `path status` can query the agent, control-plane policy/metrics/path state, control-plane peer maps, and relay HTTP APIs when validated URLs are provided, with local agent peer-map/route/status/path commands defaulting to `127.0.0.1:9780`, local relay status defaulting to `127.0.0.1:9580`, agent `peers`/`routes` reading the last successfully applied runtime peer map, and path-safe node IDs enforced before control-plane path URL construction; `stun probe` runs a typed UDP STUN Binding probe and validates unusable STUN server addresses before probing; `relay probe` runs optional-Bearer-auth relay HTTP admission plus bidirectional UDP dataplane validation and can optionally send an invalid-credential frame to verify relay dataplane abuse-drop accounting; `path activity` validates path-safe peer IDs and posts typed peer activity plus optional pin intent to the agent lazy-connect API; `path probe` validates path-safe peer/relay-node IDs, measured metrics, direct-state candidate kind, relay/unreachable selected-candidate shape, and relay-node placement before posting typed path state, optional selected candidate, policy/cost, and pin intent to the agent path-probe API; `docker install` and `k8s install` emit typed operational install plans for Compose and Helm deployments, including Docker route environment overrides and Kubernetes Service/API route discovery Helm values.
- The operator CLI bounds HTTP JSON response loading before deserialization for join, token revocation, status, peer-map, route, relay, path-status, peer-activity, and path-probe requests, including both advertised `Content-Length` and chunked body growth.
- Persistent agent node state with bounded state-file loading, owner-only state directory creation, owner-only atomic state-file replacement, symlink rejection, and group/world-access rejection for identity/WireGuard private keys, STUN candidate collection, NAT classification status, agent status/path/path-probe/STUN/NAT/peer-activity/packet-flow HTTP routes with validated measured latency/loss/jitter/stability scoring, selected-candidate ownership/address-family/usability checks, direct-state candidate-kind checks, relay/unreachable path-shape checks, runtime path-state storage rejection for nonlocal/self path keys plus invalid relay/direct/unreachable shape or selected-candidate data, optional path pinning, optional source/protocol/port/detector/application/conntrack-status/TCP-state metadata, conntrack lifecycle classification, application classification, and JSON/Prometheus/OTLP metrics, including Prometheus zero-filling for known packet-flow and path-state label series, for last-applied peer-map sync presence, peer count, advertised-route count, and peer-map generation time, and `iparsd agent`.
- Agent WireGuard key rotation endpoint that generates a new local WireGuard keypair, signs the previous-to-next public-key transition with the persisted node identity key, submits it to the configured control-plane failover set, bounds control-plane rotation response loading before deserialization, persists the accepted private key through the owner-only state store, and updates the running agent status state.
- `iparsd agent --join-token` and bounded regular-file `--join-token-path` startup registration that uses persisted agent identity/WireGuard keys, current candidates, ordered failover across token bootstrap control-plane endpoints, and runtime validation of token-derived control-plane and signal bootstrap endpoints as absolute HTTP(S) URLs before use, with the Helm chart wiring the join token Secret into the file path form.
- `iparsd agent` startup STUN discovery rejects unusable explicit `--stun-server` values, merges the remaining explicit servers with token STUN bootstrap endpoints, resolves `udp://host:port` bootstraps, filters unusable resolved socket addresses, deduplicates them in priority order, and runs bounded STUN probing or multi-server NAT classification before registration so signed bootstrap STUN services can produce initial reflexive candidates without blocking agent registration when a bootstrap STUN service is unavailable.
- `iparsd agent` heartbeat reporting that posts current node health, including managed userspace WireGuard process degradation/failure state, candidates, optional Docker/Kubernetes route advertisements, relay capability updates, optional relay status-derived capacity/session counts, and negotiated path-state data to `/v1/heartbeat` when control-plane endpoints are known, retrying across the registered endpoint and token bootstrap endpoints without stopping the agent, and omitting relay capability when relay status is unreachable or non-healthy.
- `iparsd agent` signal-service node registration that upserts the registered `NodeRecord` with refreshed endpoint candidates, relay status-derived relay capability, and managed userspace WireGuard health state across all known signal endpoints, clearing relay capability from signal when local relay status is unreachable or non-healthy and normalizing away non-admissible relay capability at the signal registry boundary.
- `iparsd agent` signal path negotiation loop that fetches peer maps with ordered control-plane endpoint failover, calls `/v1/paths/negotiate` only for active or pinned peers with ordered signal endpoint failover, fetches hole-punch plans from the signal endpoint that accepted negotiation, falls back to relay admission when direct NAT traversal setup fails and the negotiation response included usable relay candidates, downgrades failed direct NAT traversal to `UNREACHABLE` when no relay fallback is available, stores pair-scoped `PathRecord`s, removes idle relay runtime state, and includes negotiated path state in heartbeat reports.
- `iparsd agent` bounds control-plane, signal-service, and relay HTTP JSON response loading before deserialization for join, heartbeat, peer-map sync, signal node upsert, path negotiation, hole-punch plan fetches, relay status, and relay admission, with larger control-plane limits for 1000-node peer maps and tighter relay response limits for small admission/status payloads.
- Signal node registration and path negotiation carry optional NAT classification and health data, reject endpoint candidates that do not belong to the reporting/requesting node, whose kind conflicts with their address family, or whose observation timestamp is too far in the future, reject future or non-finite/out-of-range signal health reports before storing node state, reject route advertisements with the wrong owner, unsafe route IDs, zero metrics, restricted/non-canonical/duplicate CIDRs, reject future, non-finite/out-of-range, or unusable NAT classification socket data at upsert and request-time boundaries, reject desired path routes that are duplicate, non-canonical, or not advertised by the target node, apply route-specific ACL visibility to requested path routes and hole-punch-plan disclosure for target-advertised routes, drop non-admissible relay capability before storing signal node state, require both source and target nodes to be registered before negotiation, apply cluster ACL visibility to path negotiation, hole-punch-plan disclosure, and third-party relay candidate offers, prefer fresh stored source NAT classification over looser request-time classification, ignore stale NAT classifications during path selection and hole-punch planning, keep address-dependent NAT mappings eligible for `DIRECT_NAT_TRAVERSAL` when filtering evidence allows coordinated punching, avoid `DIRECT_NAT_TRAVERSAL` and suppress hole-punch reflexive candidates when either side has classified itself as relay-preferred, insufficient for hole punching, or below the configured NAT classification confidence threshold, filter stale or unusable port-zero/unspecified/multicast/IPv4-broadcast endpoint candidates before direct/IPv6/NAT traversal and hole-punch-plan selection, and offer third-party relay candidates only when they have fresh healthy signal health inside the configured relay health TTL.
- `iparsd agent` relay admission for `RELAY` paths selected by signal negotiation, optional Bearer-token authentication to relay admission endpoints, filtering port-zero, unspecified, multicast, and IPv4 broadcast local/peer session endpoints before sending admission, ranking admissible relay candidates by utilization, remaining capacity, and bandwidth, promoting the still-active relay session's relay when it remains an admissible candidate to avoid unnecessary relay churn, failing over across relay-advertised admission URLs or invalid admission responses while classifying candidate failures as missing endpoints, invalid relay capability, unavailable admission HTTP, rejected admission, or invalid response, validating returned node-pair, session ID, endpoint, expiry, and frame-encodable credential data before creating transient relay sessions, aligning pair-scoped path state to the relay that actually admitted the session, retaining still-active relay sessions when renewal fails, downgrading relay selections without usable credentials to `UNREACHABLE`, and keeping expiring relay credentials in transient agent runtime state.
- `iparsd agent` relay capability advertisement for public nodes with explicit relay endpoint/admission URL settings, startup rejection before control-plane registration for unusable relay public endpoints and unusable numeric relay admission/status URL hosts, shared relay-candidate admission checks that require a usable nonzero public endpoint and absolute HTTP(S) admission URL with a usable endpoint, gated by join-token relay policy during control-plane registration, and refreshed from healthy relay status before heartbeat or signal reports.
- Agent relay session renewal-window handling, expired relay credential purge from runtime session and forwarder state before active metrics, session accessors, renewal checks, relay forwarder endpoint resolution, or stability decisions, and stale credential removal when path negotiation returns to non-relay states.
- Agent relay dataplane forwarder that proxies local WireGuard UDP packets through credentialed relay frames, preserves opaque WireGuard payloads, drops unexpected local UDP senders, expired relay credentials, oversized datagrams, socket send failures, or non-WireGuard datagrams before forwarding them to the relay or local WireGuard socket, and counts recoverable UDP receive errors without stopping the forwarder task.
- Agent peer-map application revalidates direct selected/fallback endpoint candidates for peer ownership, direct endpoint kind, address-family consistency, and endpoint usability before WireGuard peer settings, and can bind active relay-selected peers to daemon-supervised per-peer local relay forwarder endpoints; relay forwarders support namespace placement checks, startup validation for positive capacity/backoff/crash-loop policy values, capacity limits, dead-task reaping, stale endpoint removal, restart backoff, and configurable crash-loop cooldown policy.
- Relay forwarder namespace placement startup preflight and runtime checks reuse strict `/var/run/netns` inspection for missing entries, symlinks, directories, non-`nsfs` paths, and current-process identity before accepting or binding per-peer UDP sockets.
- Agent HTTP status plus JSON/Prometheus metrics export for metrics snapshot timestamps, zero-filled path-state counts, path-probe records, relay admission attempts/successes/failures with agent-observed failure reasons, relay forwarders, userspace WireGuard process state, lazy connect, packet-flow activity, packet-flow duplicate suppression, packet-flow lifecycle and application classification including Docker API, Kubelet, CRI, and containerd management endpoints, Kerberos/RADIUS/TACACS+ authentication, BGP/BFD routing traffic, DHCP/DHCPv6 network bootstrapping, IKE/IPsec NAT-T control plus IP-in-IP/IPv6-in-IP tunnels, native ESP/AH dataplane traffic, GRE tunnels, VXLAN/Geneve overlay encapsulation, OpenVPN TCP/UDP service traffic, TURN relay-control traffic, CoAP/CoAPS edge-control traffic, NTP/NTS-KE time-synchronization traffic, SIP signaling, and bounded HTTP response header hints for management and observability APIs, and filtered destination reason counters plus bounded structured path-change event export.
- Packet-flow application classification includes zero-filled IPARS control-plane, signal, agent API, relay, STUN, and TURN counters, with HTTP API path, default service port, TLS client SNI/ALPN and server-selected ALPN, STUN magic-cookie and TURN method payload detection, and relay frame magic detection so bootstrap, path negotiation, relay admission/dataplane, and NAT probe traffic do not disappear into generic HTTP, UDP, or WireGuard buckets.
- Agent relay forwarder per-peer dataplane metrics for outbound/inbound packets, opaque payload bytes, framed datagram bytes, recoverable socket receive errors, unexpected-local-sender drops, expired relay credential drops, oversized datagram drops, socket send error drops, and non-WireGuard drop counters, exported through JSON and Prometheus metrics.
- Relay fallback network-namespace integration coverage now verifies the relay dataplane metrics path as well as payload forwarding, including invalid relay-session credential drops from a peer namespace.
- Shared `iparsd` observability bootstrap with formatted tracing output, positive metrics polling interval validation, Prometheus metrics snapshot timestamp gauges for control-plane, signal, STUN, and agent metrics plus relay status, and optional OTLP HTTP/protobuf trace/log/metrics export to OpenTelemetry collectors, including control-plane node/relay/path/stale-path/health/stale-candidate/VPN-pool/join-token-ledger/peer-map ACL visibility and metrics snapshot timestamp gauges, signal node/relay/NAT/stale-NAT/zero-filled fresh-NAT-strategy/fresh-low-confidence-NAT/NAT-confidence-threshold/health-total/health-state/stale-health/stale-candidate/request/path-negotiation-state/ACL-denial/hole-punch NAT suppression, suppression-strategy, and metrics snapshot timestamp metrics, STUN primary and RFC5780 alternate listener gauges plus server-side counters and snapshot timestamp gauge, relay JSON/Prometheus/OTLP status snapshot timestamp, capacity/e2e-only/admission failure reason/dataplane metrics, and agent path, path-probe, relay-admission failure reason, relay-forwarder, userspace WireGuard process state, lazy-connect, peer-activity, packet-flow, packet-flow duplicate suppression, packet-flow lifecycle and application classification, filtered destination reason, and metrics snapshot timestamp metric recording.
- `iparsd stun` now exposes HTTP health, JSON metrics, Prometheus metrics including snapshot timestamps, and OpenTelemetry server-side metrics for active listener state, optional RFC5780 alternate listener state, valid Binding requests, Binding responses, invalid packets, and UDP socket receive/send errors, with Docker Compose wiring the STUN HTTP healthcheck before dependent services start.
- `iparsd` agent OTLP relay-admission failure reason, packet-flow filtered reason, duplicate-source, lifecycle, and application counters are zero-filled across their known labels before first observation.
- UDP hole-punch executor and `iparsd agent` integration that validates positive punch attempt/interval settings before signal path loops start, fetches signal hole-punch plans for `DIRECT_NAT_TRAVERSAL` paths, sends coordinated UDP punch datagrams, and prevents failed direct traversal setup from being reported as a usable direct path.
- `iparsd agent` Kubernetes underlay route application for explicit Service/API CIDRs or RBAC-backed Kubernetes API Service discovery, with join-time Service/API CIDR requested-route advertisement gated by signed token route allowlists, explicit-mode startup rejection when no Service/API CIDR is supplied and API server host discovery is disabled or cannot derive a route, explicit and discovered Service/API route CIDR validation for unsafe, non-canonical, and duplicate routes before install-plan output, runtime startup, route advertisement, or backend application, stable CIDR-derived Service/API route IDs, route-loop reconciliation that removes previously applied Service/API routes and policy rules when later discovery no longer returns them and logs removed route/rule counts, Helm DaemonSet wiring for node-name discovery, default peer-map synchronization, peer-map poll interval controls, route backend selection, namespace/label filters, direct Service/API route CIDR values validation for malformed, unrestricted, unsafe IPv4, known unsafe IPv6, non-canonical IPv4, and exact duplicate routes at chart render time, legacy `serviceExposure.apiServer` value rejection in favor of `discoverApiServer`/`apiServerCidrs`, duplicate namespace and route-provider Node ID rejection, least-privilege Service discovery RBAC rendering, bounded Kubernetes Service-list response loading, and positive route intervals that are validated and gated on API discovery in both the install plan and chart template rendering, API server host-route discovery from `KUBERNETES_SERVICE_HOST` even when Service list discovery is disabled, explicit Kubernetes API URL and in-cluster API host/port validation, bounded ServiceAccount token and CA certificate file loading, explicit route-provider configuration, chart interface-name, peer-map, and route-backend validation, private registry image repository/tag/pull policy overrides, imagePullSecrets, agent hostNetwork and DNS policy controls, securityContext capability/privilege/read-only-root/seccomp and PodSecurityContext controls, persistent state hostPath plus sensitive host/container state path rejection, configurable HTTP liveness/readiness/startup probes, preStop sleep lifecycle hooks, ServiceAccount create/name/annotation values, DaemonSet pod label/annotation/priorityClass/schedulerName/runtimeClassName/nodeSelector/node-affinity/pod-affinity/toleration/topology-spread/automountServiceAccountToken/terminationGracePeriodSeconds/resource request/limit values, rollout updateStrategy/minReadySeconds/revisionHistoryLimit/maxUnavailable/maxSurge controls, relay admission bearer token SecretKeyRef injection, relay forwarder endpoint/bind/namespace controls, SYS_ADMIN and host netns mount prerequisites, supervisor controls, and opt-in PodDisruptionBudget minAvailable/maxUnavailable controls with CLI and chart validation, CLI and chart cluster control/signal URL wiring and validation including userinfo, invalid-port, and unusable numeric-host rejection, optional STUN literal socket wiring and validation, relay advertisement startup/chart validation for required endpoints, HTTP(S) admission/status URLs with chart-side userinfo, invalid-port, and unusable numeric-host rejection, literal public socket values, positive capacity, join-token Secret metadata, relay admission token Secret metadata, and chart release/namespace/nameOverride/fullnameOverride metadata, and optional agent/relay Service exposure templates that validate Helm release, namespace, chart override metadata, Service type, CLI Service and targetPort overrides, direct Service and target ports, unsafe fixed ClusterIP/clusterIPs rejection and explicit NodePorts, Service appProtocol values, publishNotReadyAddresses controls, unsafe and non-canonical LoadBalancer source-range CIDR values and LoadBalancer-only placement, LoadBalancer class and fixed IP values, unsafe fixed LoadBalancer IP/externalIP rejection, LoadBalancer health check NodePorts, LoadBalancer node-port allocation controls, Service IP family policy and family lists with fixed ClusterIP/clusterIPs family validation, internal and external traffic policy values, traffic distribution preferences, Service session affinity and integer ClientIP timeout values, chart/CLI annotation key/value handling with CLI values capped at 262144 bytes and string-only chart annotation values capped at 262144 bytes and free of control characters, optional ingress-only unsafe and non-canonical NetworkPolicy CIDR allowlist rejection with scoped hostNetwork limitation acknowledgement and port rules aligned to configured target ports, require explicit acknowledgement for NodePort/LoadBalancer exposure, require LoadBalancer source ranges unless unrestricted exposure is explicitly acknowledged, require explicit public exposure acknowledgement for externalIPs, require a scoped additional acknowledgement for `externalTrafficPolicy=Cluster`, reject inactive Service exposure-specific values including type, port, appProtocol, and annotations, reject NodePorts outside the default Kubernetes range or on ClusterIP Services, reject invalid appProtocol qualified names, reject readiness-bypass controls when their Service is not exposed, reject LoadBalancer source-range duplicates or source ranges combined with unrestricted LoadBalancer acknowledgement, reject irrelevant exposure and traffic-policy acknowledgement values, reject non-default `externalTrafficPolicy` values outside NodePort/LoadBalancer Services, reject LoadBalancer classes outside Kubernetes qualified-name syntax or on non-LoadBalancer Services, reject fixed LoadBalancer IPs on non-LoadBalancer Services, reject health check NodePorts outside LoadBalancer Services using `externalTrafficPolicy=Local`, reject health check NodePorts that reuse Service NodePorts, reject disabled LoadBalancer node-port allocation outside LoadBalancer Services or combined with explicit Service NodePorts, reject duplicate relay UDP/HTTP NodePorts and duplicate explicit NodePort/health-check NodePort allocations across agent/relay Services, reject duplicate or policy-inconsistent IPv4/IPv6 Service family lists, and support internal/external traffic policy plus traffic distribution.
- Direct Helm values for agent and relay Service and target ports, NodePorts, LoadBalancer health-check NodePorts, session-affinity timeouts, peer-map cadence, relay-forwarder supervision limits, relay advertisement capacity, Kubernetes route intervals, probe/lifecycle/scheduling fields, and rollout/PodDisruptionBudget integer-or-percent values are validated as bounded non-negative integers or bounded integer-percentage values before template `int` conversion, rejecting non-numeric strings, fractional values, oversized numeric values, or empty required numeric fields instead of relying on template coercion.
- `ipars k8s install` rejects Service type, targetPort, or LoadBalancer source-range overrides for inactive agent API or relay Services before emitting Helm values, matching the chart's direct-value inactive exposure checks for service exposure-specific settings.
- `iparsd agent` Docker container CIDR route application from explicit namespace/interface/CIDR intents or Docker Engine API bridge-network discovery, with join-time Docker CIDR requested-route advertisement gated by signed token route allowlists, API version, route interval, Linux interface/namespace, network filter validation, explicit-mode startup rejection when namespace or container CIDRs are missing, explicit and discovered container CIDR unsafe-range rejection for unrestricted, unspecified, loopback, link-local, multicast/broadcast routes, non-canonical explicit/discovered CIDR route rejection, duplicate and overlapping explicit/discovered CIDR route rejection, route-loop reconciliation that removes previously applied container routes or policy rules when later explicit/API discovery plans omit them and logs removed route/rule counts, targeted diagnostics for requested filters that are absent, non-bridge, or subnetless, Docker API Unix socket runtime preflight, discovered bridge-network name and ID validation before route intent namespace labeling, duplicate discovered network name/ID rejection, ambiguous explicit CIDR/discovery rejection, inactive Docker route-setting rejection, discovery-required API socket and network filters, duplicate network filter rejection, network name/ID filters, API-order-independent discovered namespace names, stable CIDR-derived Docker route IDs, rootless absolute dot-component-free socket discovery, non-Unix, relative, or dot-component `DOCKER_HOST` rejection for local socket discovery, userspace WireGuard peer-configuration backend selection for rootless interfaces with optional daemon-managed external process launch/readiness/shutdown, HTTP-over-Unix-socket Docker API discovery coverage with bounded network-list response loading for multi-network route intent construction, Docker Compose wiring with env-driven route mutation, route backend selection, WireGuard backend/process selection/readiness/shutdown timeout settings, RFC5780 STUN alternate listener exposure, relay daemon public endpoint/admission URL advertisement settings, relay admission Bearer-token pass-through, relay admission session cap/rate-limit controls, and agent relay forwarder endpoint/bind/WireGuard endpoint/netns/capacity/restart/crash-loop controls, file-backed Compose join-token secret wiring through `IPARS_AGENT_JOIN_TOKEN_PATH`, discovery-only Compose Docker API socket override through `docker/compose.docker-discovery.yaml`, `IPARS_DOCKER_API_SOCKET`, and `IPARS_DOCKER_API_SOCKET_HOST` with a read-only non-creating bind mount, service healthchecks and host-network agent loopback endpoints for co-located control-plane/signal/relay services, gated Docker Compose smoke coverage that verifies the base manifest omits the socket bind and the discovery override renders the requested host socket as read-only with `create_host_path=false`, multi-network/rootless/userspace-WireGuard/relay-forwarder/STUN alternate-listener environment overrides, builds the repository image, generates a signed join token, starts PostgreSQL/control-plane/signal/STUN/relay/agent with `docker compose up --wait`, and verifies Compose service state using an agent `dry-run` runtime override, and `docker install` plan output with matching Docker discovery validation, unsafe/non-canonical/duplicate/overlapping explicit CIDR route rejection, duplicate multi-network filter rejection before comma-delimited Compose rendering, absolute dot-component-free explicit Docker API socket path validation, relay forwarder endpoint/bind/WireGuard endpoint/netns/supervisor validation with environment wiring and CAP_SYS_ADMIN plus host `/var/run/netns` prerequisites for namespaced forwarders, args-aware Docker API socket preflight commands for explicit/default rootful discovery plans that reject relative, dot-component, symlinked, or non-socket Docker API paths before Compose startup, configurable host-route advertisement, selectable command/kernel-netlink route backend environment wiring, positive route-loop interval environment wiring, and RFC5780 STUN alternate-listener environment wiring, bounded bare-command-or-absolute-path userspace WireGuard command tokens, at-most-128 control-character-free and comma-free 4 KiB userspace WireGuard arguments for the comma-delimited Compose environment, positive one-hour-capped userspace WireGuard lifecycle timeout validation, shell-quoted Compose manifest paths, default explicit Compose CIDR settings when rootful discovery is disabled, healthcheck/loopback/join-token-secret/discovery-only socket-bind/relay-admission-control/relay-forwarder notes, multi-network route environment settings, and rootless install-plan guards that require `--userspace-wireguard-command`, export `IPARS_AGENT_WIREGUARD_BACKEND=userspace-command`, `IPARS_AGENT_APPLY_DOCKER_ROUTES=false`, and `IPARS_DOCKER_DISCOVER_NETWORKS=false`, omit Docker route environment, and reject Docker route/discovery flags plus namespaced relay-forwarder placement.
- Docker API route discovery rejects ambiguous network filters that match multiple Docker networks by name or ID before constructing route intents.
- Docker discovery install-plan preflight inspects requested Docker networks before Compose startup and rejects missing, non-bridge, subnetless, or duplicate-IPAM-subnet filters before the agent starts.
- Rootless Docker Compose install-plan rendering adds `docker/compose.rootless.yaml`, pins the rootless override to the userspace WireGuard backend with Docker route application and Docker network discovery disabled, resets agent/relay Linux capability additions and `/dev/net/tun` device mounts, requires a managed userspace WireGuard command, and rejects static-CIDR, Docker API discovery route plans, or namespaced relay-forwarder placement that would require capabilities or host namespace access the rootless override removes.
- Control-plane heartbeat handling validates and persists signed node health, refreshed endpoint candidates, optional route advertisement refreshes, relay capability, and pair-scoped local-node path-state snapshots in memory, SQLite, and PostgreSQL stores, rejecting invalid health metrics, duplicate pair-scoped path states, unregistered or ACL-hidden path peers, stale selected path candidates, malformed path scores, and future health/candidate/path timestamps before persistence, replacing each reporting node's previous paths to clear stale idle/restart state, and filtering path records older than the configured path-state TTL from status and active metrics.
- Linux WireGuard command backend for interface creation and peer upsert/removal through explicit `ip`/`wg` commands, with optional validated `ip netns exec` execution.
- Linux interface-name validation rejects empty, oversized, path-like, `.`/`..`, and option-like `-`-prefixed values before daemon preflight, install-plan rendering, route-plan validation, or Docker route intent application reaches command or netlink backends.
- Selectable Linux kernel WireGuard netlink backend for peer-map application, using rtnetlink for interface creation/up state and WireGuard generic netlink for peer upsert/removal without invoking `wg`, in either the current namespace or validated `--linux-netns` placement.
- Selectable userspace WireGuard command backend for peer-map application that validates a userspace interface with `wg show`, optionally starts and supervises an external userspace WireGuard process with inherited environment variables cleared and only a fixed system `PATH` plus `C` locale restored before peer-map sync, records lifecycle state for agent status/metrics/OTel and heartbeat/signal health, logs unexpected process exits, stops and reaps it on readiness failure or shutdown, and applies peer updates through `wg set` without creating a kernel WireGuard link.
- Linux route-manager command backend for route replacement/removal and policy-rule add/delete through explicit `ip` commands, with optional validated `ip netns exec` execution.
- Selectable Linux route-manager rtnetlink backend for peer-map, Docker, and Kubernetes route plans, including route replacement/removal and policy-rule add/delete without invoking `ip`, in either the current namespace or validated `--linux-netns` placement; namespaced rtnetlink socket creation rejects missing, symlinked, directory, or non-`nsfs` namespace paths before `setns` and restores the caller's thread namespace through normal error and unwind paths; Docker route intents are revalidated for unsafe, non-canonical, duplicate, and overlapping container CIDRs before dry-run, Linux command, or kernel-netlink application, and Kubernetes underlay route intents are revalidated for unsafe, non-canonical, and duplicate Service/API CIDRs while still allowing specific host routes inside broader Service CIDRs.
- Docker route intents reject invalid container network namespace labels before plan generation reaches dry-run, command, or kernel-netlink backends, and generated Docker/Kubernetes route plans are validated before checked plan helpers return them.
- Generic route-manager `RoutePlan` validation rejects invalid Linux interface names, empty/oversized/path-unsafe/duplicate route IDs, zero route metrics, unrestricted/unsafe/non-canonical/duplicate route CIDRs, invalid policy-rule table or priority values, duplicate policy rules, reused policy-rule priorities, explicit zero fwmark selectors, unsafe or non-canonical policy-rule from/to selectors, and mixed selector address-family combinations before dry-run, Linux command, or kernel-netlink backends run.
- Gated Linux network namespace integration smoke tests for applying and removing routes through the namespaced command and rtnetlink route backends, plus applying/removing Docker container and Kubernetes Service/API route intents through the namespaced command backend.
- Gated Linux network namespace integration smoke test for creating a WireGuard interface and upserting/removing a peer through the kernel WireGuard netlink backend inside a target namespace.
- NAT classification and signal path-selection coverage for address-dependent mapping from same-port, different-address STUN probes, plus gated Linux network namespace integration smoke tests for signal-registry generated `DIRECT_NAT_TRAVERSAL` plans executed by the UDP puncher across direct-routed isolated namespaces, endpoint-independent two-sided SNAT, two-sided fixed-port SNAT, mixed port-preserving/fixed-port two-sided SNAT, one-sided fixed-port public-peer SNAT, one-sided port-preserving public-peer SNAT, symmetric and asymmetric address/port-dependent two-sided SNAT non-traversal, and one-sided address/port-dependent public-peer SNAT non-traversal topologies, fixed-port and port-preserving one-sided public-peer SNAT, one-sided address/port-dependent public-peer SNAT non-traversal, two-sided endpoint-independent SNAT topologies with IP-only, fixed public-port, and mixed port-preserving/fixed-port mappings, plus symmetric and asymmetric address/port-dependent SNAT non-traversal cases where advertised reflexive ports differ from peer-destination mappings.
- Gated Linux network namespace integration smoke test for relay fallback dataplane forwarding between isolated client, relay, and peer namespaces, including rejection of an invalid relay credential frame before the accepted opaque payload is delivered.
- Agent peer-map applier that turns active or pinned `PeerMap` records into WireGuard peer configs, endpoint choices, peer host routes, and peer-owned advertised route plans, while pruning idle/stale unpinned peers from WireGuard state, ignoring advertised routes whose owner does not match the peer record, and removing previously applied peer or advertised routes that disappear from later peer maps.
- `iparsd agent --apply-peer-map` continuous peer-map polling that fetches the control-plane peer map with ordered endpoint failover, applies active/pinned peers through selectable `linux-command` or `dry-run` runtime backends when explicitly enabled, supports command, userspace-command, and kernel-netlink WireGuard peer application with `--linux-netns` namespace placement where applicable, and retries without stopping the agent when the control plane is temporarily unavailable.
- Linux command/runtime preflight and execution hardening for `iparsd agent` that always validates static runtime configuration such as Linux interface names, namespace names, daemon poll/route/renew intervals, a positive runtime command timeout capped at one hour, command output bounds with a 1 MiB per-stream maximum, userspace WireGuard launch settings with bounded bare-command-or-absolute-path command tokens that reject control characters, whitespace, path-special components, and option-prefixed program names, at-most-128 control-character-free 4 KiB arguments, and one-hour-capped ready/shutdown lifecycle timeouts, relay-forwarder capacity, relay-forwarder endpoint/WireGuard endpoint usability, required forwarder endpoint pairings, inactive Linux runtime backend/namespace selectors, and inactive forwarder namespace settings, then validates required `ip`/`wg`/userspace-WireGuard commands through absolute PATH entries that reject path-special components and executable regular files without file, parent-directory, or ancestor-directory symlink indirection, rejects executable files, direct parent directories, and ancestor directories that are group/world-writable or not owned by root or the current effective user, checks selected `NETLINK_ROUTE`/`NETLINK_GENERIC`/`NETLINK_NETFILTER` socket readiness for kernel-netlink and conntrack detectors, Docker API Unix socket readiness for discovery-backed Docker route plans, bounded forwarding sysctl reads for Docker/Kubernetes route-provider traffic (`net.ipv4.ip_forward=1` and explicit IPv6 `net.ipv6.conf.all.forwarding=1`), bounded `/proc/self/status` capability reads for `CAP_NET_ADMIN` when host networking will be mutated or conntrack netlink is enabled, `CAP_NET_RAW` only for kernel WireGuard peer-map dataplane application, `CAP_SYS_ADMIN` when Linux namespace placement, namespaced userspace WireGuard commands, or relay-forwarder namespace placement are requested, and requested data-plane or relay-forwarder `/var/run/netns` namespace entries before starting data-plane application loops, rejecting path-special `.`/`..` or option-prefixed namespace names, missing namespace entries, symlinks, directories, and non-`nsfs` regular files while warning on current-namespace placement, bounds command-backed `ip`/`wg` child processes with a configurable timeout, explicit timeout process-group kill/reap handling in both agent WireGuard and route-manager route command runners, per-stream output capture limit while still draining stdout/stderr pipes, sanitized child environments for both command runners and managed userspace WireGuard process launch with inherited variables cleared and only a fixed system `PATH` plus `C` locale restored, managed userspace WireGuard process-group shutdown that first sends graceful termination, then force-kills and reaps child process trees after the shutdown timeout instead of only killing the direct child, managed userspace WireGuard readiness failures that retain the last failed `wg show` readiness check in the returned error and runtime process status, and revalidates Linux command and route command runtime bounds plus command vectors at runner entry for positive one-hour-capped timeouts, positive 1 MiB-capped output limits, non-empty programs, NUL-free/control-character-free/whitespace-free bare-or-absolute program tokens without path-special components or option-prefixed names, NUL-free argument strings, argument-count caps, and argv byte caps; the command and route command runners now resolve bare programs through the fixed sanitized PATH or absolute program paths to trusted non-symlink executable files, reject unsafe executable or parent-directory ownership/permissions before spawn, reject command-file and parent-directory symlinks, rewrite `ip netns exec` inner programs to trusted absolute paths so namespace-wrapped commands do not rely on a later child PATH lookup, managed userspace WireGuard process launch re-resolves both the outer command and namespaced inner command to trusted absolute paths immediately before spawn and revalidates the final argv count/size/NUL shape after namespace wrapping, and escape command labels, managed userspace WireGuard process labels, plus captured stderr snippets in failure/timeout diagnostics so rejected or failed command vectors cannot inject control characters into operator logs.
- Runtime backend preflight now checks required process capabilities before selected kernel netlink probes, with coverage for kernel-netlink WireGuard/route application and dry-run conntrack-netlink detectors so missing privileges surface as explicit `CAP_NET_ADMIN`/`CAP_NET_RAW` failures before lower-level socket errors.
- Runtime command preflight now treats every `PATH` entry as part of the trusted lookup chain, rejecting missing, symlinked, non-directory, group/world-writable, or untrusted-owner entries even when the requested `ip`, `wg`, or userspace WireGuard command would be found later in `PATH`.
- Namespaced route-manager and agent command runners now perform best-effort current-namespace diagnostics before wrapping commands with `ip netns exec`, surfacing accidental current-process namespace placement without changing the underlying command execution behavior when daemon preflight is bypassed.
- Route-manager kernel-netlink namespace socket setup validates the selected `/var/run/netns` entry before switching namespaces and now warns when the target namespace file resolves to the current process namespace, giving library-level diagnostics even when callers bypass daemon preflight.
- Relay-forwarder namespace placement validates the selected `/var/run/netns` entry at startup and rechecks the current process namespace before binding the forwarder socket, with tests covering symlinked namespace targets, same-namespace identity acceptance, and mismatched current namespace rejection.
- `iparsd agent --runtime-backend dry-run` for peer-map, Docker route, and Kubernetes underlay loops using in-memory WireGuard state and dry-run route application without mutating host networking.
- Lazy connect and pinning runtime support in the agent crate and daemon loops, including peer activity recording, packet-flow destination resolution against peer VPN IPs and peer-owned advertised routes, optional typed packet-flow source/protocol/TCP-UDP-SCTP nonzero-port/bounded ASCII-token detector/protocol-compatible application/bounded-payload-prefix/bounded-normalized-conntrack-status/TCP-state metadata, shared packet-flow transport metadata validation for agent HTTP, eBPF JSONL, and internal runtime inputs, inferred conntrack lifecycle and detector-hint, payload-prefix, or port-based application classification metrics for common control-plane, runtime-management, routing, database, observability, messaging, mail, cache, search, directory, authentication, time-synchronization, VPN, overlay, file-transfer, file-sharing, and remote-admin protocols, bounded first-party payload-prefix parsers for DNS wire-format queries, DHCP BOOTP and DHCPv6 option prefixes, IKEv2, NAT-T non-ESP marker headers, and ESP-in-UDP headers, VXLAN and Geneve tunnel headers, Kerberos DER requests/replies, RADIUS packets, TACACS+ headers, BGP message headers, BFD control headers, NTP v3/v4 client/server packets, HTTP/Kubernetes API/Kubelet/Docker API/Prometheus/OTLP/Jaeger/Loki/Tempo/Zipkin/ClickHouse/InfluxDB/etcd/Consul/Vault/Nomad/Elasticsearch/gRPC including HTTP/2 preface or mid-stream payload hints, TLS handshakes with bounded ClientHello SNI/ALPN hints for Kubernetes API, Kubelet, Docker API, CRI, containerd, etcd, ZooKeeper, Consul, Vault, Nomad, Prometheus, OpenTelemetry/OTLP, Jaeger, Loki, Tempo, Zipkin, ClickHouse, InfluxDB, Elasticsearch, database, messaging, mail, cache, directory, authentication, NTS-KE time-synchronization, file-transfer, file-sharing, and remote-admin endpoints, QUIC v1 long-header checks, SSH identification checks, WireGuard UDP message type/length checks on arbitrary ports, TURN relay-control method checks, CoAP UDP header, option, and payload-marker checks, LDAP BER envelope checks, SMB direct-TCP/header checks, Kerberos KDC/password-change, RADIUS/RadSec, TACACS+, BGP/BFD, DHCP/DHCPv6, IKE/IPsec NAT-T, IP-in-IP/IPv6-in-IP/GRE/native ESP/AH protocol hints, VXLAN/Geneve, OpenVPN, TURN/TURNS, CoAP/CoAPS, NTP/NTS-KE, NFS, Syslog, FTP/FTPS, TFTP, rsync/rsyncd, SMTP/IMAP/POP3, VNC/RFB, SIP/SIPS, and SNMP service port hints, SNMP BER first-packet checks, RDP TPKT/X.224 checks, VNC/RFB version-banner checks, FTP/FTPS greeting and control-command checks, TFTP RRQ/WRQ request checks, rsync daemon greeting checks, SMTP/IMAP/POP3 greeting and command-line checks, SIP request/status-line checks, PostgreSQL startup messages with bounded parameter fields, framed frontend message sequences with bounded SQL query prefixes and Bind parameter tails, and bounded backend Authentication, ParameterStatus, BackendKeyData, ReadyForQuery, CommandComplete, ErrorResponse, and NoticeResponse frames, MySQL server handshake, client handshake response with bounded database/plugin/connection-attribute fields, SSLRequest, command packets, and bounded server OK, ERR, EOF, and AuthSwitchRequest packets, SQL Server/TDS prelogin/login/sql-batch packets plus bounded server DONE, ERROR, and INFO response token streams, Oracle/TNS connect descriptors, ClickHouse HTTP header hints, bounded native ClientHello, ServerHello, Query, Exception, Progress, Pong, and EndOfStream packet checks, and HTTP/native service ports, InfluxDB v2 HTTP API paths, Kubernetes API resource/OpenAPI HTTP paths, Kubelet pod/stats/cAdvisor/resource metrics paths, Docker Engine versioned and unversioned management API paths, CRI runtime/image gRPC method paths, containerd content/image/container/task/snapshot/namespace/event gRPC method paths, etcd v2/v3 HTTP API paths and v3 gRPC method paths, version/build header hints, and HTTP service port, ZooKeeper four-letter commands and connect requests, Consul HTTP API paths and RPC/API/gossip service ports, Vault HTTP API paths and API/cluster service ports, Nomad HTTP API paths and API/RPC/Serf service ports, Jaeger collector/query/sampling HTTP API paths and collector/query/agent service ports, Loki HTTP API paths and HTTP/gRPC service ports, Tempo trace/search/metrics HTTP API paths and HTTP service port, Zipkin collector/query/UI HTTP API paths, HTTP/gRPC collector hints, and HTTP service port, Redis RESP array commands, selected inline commands, and bounded scalar server replies, Memcached text/binary command frames plus bounded text server status, value, and stat responses, Kafka length-prefixed request-header checks, NATS control-line subject/wildcard checks, MQTT CONNECT and bounded CONNACK, CoAP, AMQP, Cassandra, MongoDB, and Elasticsearch transport frames with stricter MQTT remaining-length/name/flag/client-id checks, AMQP version header/frame and method class-channel checks, Cassandra version/flag/opcode/body-shape checks, MongoDB opcode-specific section/body checks, and Elasticsearch transport status/variable-header checks, payload-prefix precedence over generic web-port guesses while preserving well-known service ports for generic HTTP/TLS observations, detector-specific option rejection for inactive packet-flow detectors, opt-in Linux conntrack procfs polling with configurable byte, line, and parsed-flow bounds plus existing custom-path regular-file/symlink rejection, bounded conntrack netlink table dumping, bounded conntrack NEW/UPDATE event subscription with `CTA_STATUS` and TCP protocol-info lifecycle metadata, bounded eBPF-exporter JSONL event-file ingestion with the same typed packet-flow schema and payload-aware application hints or samples plus existing-path regular-file/symlink rejection, in-process Aya eBPF object loading/tracepoint attach/ring-buffer packet-flow ingestion with a shared fixed binary event ABI, unsupported event/protocol/TCP-state/protocol-state/protocol-port/conntrack-status/unused-field/IPv4-padding rejection, and bounded per-wakeup draining, repository-built eBPF object for outbound `connect(2)`/`sendto(2)` destination events, preflight validation for non-empty regular eBPF object files and tracepoint IDs under tracefs, a gated privileged attach/event smoke test for real `sendto(2)` ring-buffer events, shared runtime filtering and reason metrics for unspecified/loopback/multicast/broadcast/link-local destinations across HTTP and detector inputs plus HTTP packet-flow responses that report filtered reasons for rejected or unmatched destinations, detector-side duplicate flow suppression with lifecycle- and application-aware fingerprints and source-labelled metrics, score-reason reporting for stability, relay-to-direct promotion margin to avoid path flapping only while a relay credential is still active, default control-plane role plus route-provider/Kubernetes-control-plane tag pins, observed owned-route/relay/path pins, active-only signal negotiation, and idle peer removal during peer-map application.
- Packet-flow application classification now has dedicated OpenSearch and Solr application buckets with zero-filled agent metrics, typed hint support, bounded OpenSearch HTTP `_plugins`/`_opendistro` path and `X-OpenSearch-*` response-header detection, bounded Solr `/solr`, `/api/collections`, `/api/cores`, and `X-Solr-*` detection, TLS SNI/ALPN hints, and Solr TCP 8983/8984 service-port hints, while generic Elasticsearch-compatible search paths continue to use the existing Elasticsearch fallback.
- Packet-flow application classification now has a dedicated Git application bucket with zero-filled agent metrics, typed hint support, bounded Git smart HTTP `.git/info/refs?service=git-*` and `.git/git-*` RPC path detection, native `git://` pkt-line service request detection, TLS SNI/ALPN hints, and TCP 9418 service-port hints.
- Packet-flow runtime filtering now rejects internally submitted inconsistent transport metadata, including unusable source IP metadata, ACL wildcard `Any` used as a packet observation protocol, zero ports, oversized, empty, whitespace-padded, non-token, or control-character-bearing detector names, oversized payload-prefix samples, oversized conntrack status vectors, and unsorted or duplicate conntrack status flags, before lazy-connect activation, pinning, or lifecycle/application classification metrics, and exports the `inconsistent_transport_metadata` filtered reason for operator visibility; eBPF ring-buffer events normalize all-zero source storage to an unknown source.
- DNS packet-flow classification includes standard DNS wire-format queries with bounded declared question-count validation, standard-query answer/authority section rejection while allowing validated EDNS OPT/additional records with root owners, EDNS version zero, and bounded flags, response resource-record count, class, A/AAAA RDATA-length, NS/CNAME/MB/MG/MR/SOA/PTR/MX/MINFO/RP/AFSDB/RT/PX/SRV/KX/NAPTR/NSEC/DNAME RDATA-name validation, HINFO/TXT/NAPTR character-string RDATA validation, SSHFP/TLSA/SMIMEA/URI service-security RDATA validation, SVCB/HTTPS priority/target/parameter RDATA validation, CAA tag RDATA validation, DS/DNSKEY/RRSIG DNSSEC RDATA header/length validation, and NSEC/NSEC3/NSEC3PARAM denial-record bitmap/hash/salt RDATA validation after questions or in question-less responses such as mDNS answers, bounded DNS name-compression pointer validation for response owner names, DNS-over-TCP length-frame bounds with max-prefix truncation handling, the standard encrypted DNS transport port 853 for TCP and UDP, conservative DNS resolver TLS SNI and `dot`/`doq` ALPN hints, plus DNS-over-HTTPS and Oblivious DNS-over-HTTPS `/dns-query`, `application/dns-message`, and `application/oblivious-dns-message` HTTP request/response hints, preventing DNS and encrypted DNS observations from falling through to generic HTTP/HTTPS/QUIC classification.
- Packet-flow payload-prefix classification recognizes bounded OpenVPN plaintext control-channel reset, control, and ACK packet prefixes over UDP and TCP length-framed transports, so non-standard-port VPN handshakes classify before generic port fallback.
- Packet-flow payload-prefix classification recognizes bounded FTP control-channel greetings/commands plus structured server replies for passive-mode endpoints, transfer state, login/password/account prompts, and file-action results, TFTP RRQ/WRQ requests plus ERROR/OACK response packets, rsync daemon greetings and bounded negotiation/control lines, NFS ONC RPC call frames, and Syslog RFC3164/RFC5424 messages, in addition to existing port and TLS SNI/ALPN hints for file-transfer, file-sharing, and logging traffic.
- Packet-flow payload-prefix classification recognizes conservative SMTP reply lines for service greetings, EHLO extension keywords, DATA continuation prompts, and enhanced-status responses, while leaving ambiguous bare status lines such as `250 OK` unknown on non-standard ports.
- Packet-flow payload-prefix classification recognizes conservative IMAP server responses for IMAP4 capability banners, tagged command completions, and standard bracketed response codes, while leaving ambiguous untagged mailbox state lines unknown on non-standard ports.
- Packet-flow payload-prefix classification recognizes conservative POP3 server responses for greetings, STAT/drop listings, capability/listing/message-following replies, octet count replies, and standard extended response codes while leaving ambiguous bare `+OK` lines unknown and preserving existing Redis error reply classification for bare `-ERR` lines on non-standard ports.
- Agent packet-flow runtime metrics cover non-standard-port payload-prefix classification for TURN, CoAP, NATS, MQTT, AMQP, Cassandra, and MongoDB through lazy-connect route matches.
- eBPF ring-buffer packet-flow attach specifications are statically bounded to 32 unique `PROGRAM:CATEGORY:NAME` tracepoints so duplicate attachments and unbounded CLI/env expansion are rejected before loader preflight, tracepoint `id` preflight rejects symlink, non-file, empty, oversized, non-numeric, or zero values under tracefs, and ring-buffer loader config generation revalidates the map identifier plus non-empty bounded unique attachments before object loading.
- Packet-flow detector read limits are statically capped before polling, table dumping, JSONL ingestion, or ring-buffer draining, with 64 MiB text reads, 64 KiB text lines, 1,048,576 parsed records, 65,536 eBPF ring-buffer events per wake, detector config generation revalidation for procfs, conntrack-netlink, eBPF JSONL, and eBPF ring-buffer limits, duplicate-suppression TTL values either disabled with zero or capped at 24 hours, and retained duplicate-suppression fingerprints capped at 1,048,576 entries.
- Relay session table that forwards only expiring credentialed non-empty opaque payload frames, bounds relay-frame metadata and payload sizes, rejects malformed empty relay frames, and enforces per-session throughput windows.
- Docker Compose and Helm chart starting points aligned to the current plain-HTTP `iparsd` service listeners, with Compose readiness checks for PostgreSQL and HTTP services plus env-interpolated Docker route, route backend, WireGuard backend/userspace lifecycle, relay forwarder, and discovery-only socket-bind settings, Helm peer-map sync plus route backend selection, Docker build context pruning through `.dockerignore`, locked Rust 1.88 image builds with BuildKit cache mounts, and gated Compose smoke validation.
- `ipars-load` executable scale/load harness for 3-node, 10-node, and 1000-node in-memory control-plane/signal scenarios, loopback HTTP endpoint transport for control-plane join/peer-map and signal upsert/negotiate paths with bounded JSON/text HTTP response loading, relay HTTP admission plus UDP forwarding throughput runs with validated packet/payload bounds and final relay capacity/E2E/policy/admission counter snapshots including failure reasons plus dataplane receive/forward/drop and invalid-credential abuse-drop counters cross-checked against Prometheus drop-reason samples, report-level success gates for registration counts, agent status endpoint coverage, agent endpoint-candidate coverage, agent runtime path-state endpoint coverage after peer-activity-driven signal negotiation, control-plane path-state metrics and `/v1/paths/{node_id}` status propagation from agent heartbeats, advertised route counts, peer-map edge counts, cross-control-plane peer-map consistency, control-plane survivor failover with survivor path-state metrics and `/v1/paths/{node_id}` status revalidation, post-failover existing relay UDP session delivery, control-plane metrics endpoint coverage, path negotiation reachability, relay-candidate min/max consistency, relay packet delivery, relay dataplane invalid-credential drop visibility, relay session/capacity/policy/E2E/admission counter consistency, zero-filled relay failure-reason snapshots, retained runtime manifest completed-phase/timestamp/workload/readiness-timeout/HTTP-and-UDP-endpoint/child-role-order/PID-presence-uniqueness/exit-status-code/process/log-diagnostic/measurement/permission/owner consistency with post-shutdown retained log byte-count/tail-hash verification, duplicate/empty/role-mismatched/serial/order-skewed retained child log rejection, retained join-token/agent-state/temporary-manifest residue rejection, unexpected retained runtime artifact rejection, and daemon health consistency, and spawned multi-process `iparsd` daemon transport across one or more shared-SQLite control-plane processes, signal, RFC5780 alternate-listener STUN, relay, and dry-run agent processes with validated control-plane/agent-process bounds and readiness timeout bounds, owner-only runtime directories for agent state compatibility, scenario-matched relay/route-provider join-token policy distribution across all runtime control-plane bootstrap URLs, relay-role agent advertisement of the colocated daemon relay endpoint/admission/status/capacity, route-provider dry-run Docker CIDR route advertisement matching signed route allowlists, child liveness checks, post-write validated owner-only regular private join-token file handoff through `--join-token-path` with post-readiness scrubbing, post-shutdown agent-state scrubbing, retained regular runtime file owner-only hardening, owner-only per-child stdout/stderr log capture, optional retained runtime directories plus owner-only atomically replaced token-redacted run manifests that record startup, startup-readiness, readiness timeout settings, STUN HTTP plus primary and alternate STUN UDP endpoints, control-plane failover, measurement phase progress, and completed relay/failover dataplane counters with relay invalid-credential drop evidence from JSON status and Prometheus plus workload/options snapshots, run-start and last-update timestamps, and exited-child unique PID/status/code plus log byte-count/log-tail SHA-256 diagnostics for post-failure analysis, readiness-failure manifest refresh, log-tail diagnostics on liveness/readiness failures, service health, control-plane registration, peer-map visibility, healthy-node and path-state per-endpoint min/max HA consistency metrics, signal health metrics, signal negotiation readiness waits, final JSON control-plane/signal/STUN/relay capacity, STUN Binding request/response, and admission snapshots, and gated `IPARS_TEST_IPARSD_BIN` external-daemon smoke coverage for the relay-candidate and retained completed-manifest contracts.
- Daemon transport child `iparsd` processes now clear inherited environment variables and restore only a fixed system `PATH` plus `C` locale before spawn, preventing operator or CI secret environment variables from entering retained child process context.
- Daemon load retained runtime manifests record the canonical `iparsd` binary path, byte count, SHA-256 digest, per-child redacted argv, per-child argv hashes, and per-child start/exit/runtime timing, and completed-run validation uses bounded regular-file manifest reads plus tail-only log diagnostics before rechecking binary identity, child subcommand alignment, argv hash integrity, secret-like argument redaction, and child lifecycle timestamp order before accepting retained manifest evidence.
- `scripts/load-smoke.sh` runs repeatable load-harness smoke checks for 3/10/1000-node in-memory scenarios, 3-node loopback HTTP transport, 3-node relay-UDP dataplane transport, report transport/scenario identity validation, and optional daemon transport with either a provided `iparsd` binary or `IPARS_LOAD_SMOKE_BUILD_DAEMON=1` local daemon build.
- `scripts/netns-smoke.sh` runs a repeatable privileged namespace smoke suite with temporary namespace preflight that validates namespace creation, `ip netns list` visibility, non-symlink `/var/run/netns` entry creation, loopback setup, and cleanup removal before running route-manager namespace tests including namespace guard setup/listing/non-symlink entry/teardown cleanup validation, optional WireGuard namespace tests when `wg` is installed, UDP hole-punch namespace tests, and relay-fallback namespace tests.
- `scripts/docker-smoke.sh` runs the gated Docker Compose smoke suite with Docker daemon and `docker compose` CLI preflight before building the repository image and starting the dry-run PostgreSQL/control-plane/signal/STUN/relay/two-agent Compose stack, with smoke-selected host TCP/UDP ports reserved until immediately before Compose startup to avoid self-collisions and reduce external port races, then verifies control-plane, signal, STUN, relay, and agent HTTP API payloads after service health, two-agent control-plane registration plus reciprocal control-plane and agent-local peer-map visibility, packet-flow-triggered lazy-connect with typed observation metadata, valid no-overlay packet-flow filtering, and agent packet-flow/lazy-connect JSON metrics plus aggregate, filtered-reason, and lifecycle/application-labelled Prometheus metrics, peer-activity pinning plus agent path-state creation through signal negotiation and signal path-negotiation metrics, heartbeat propagation of agent path state back to control-plane `/v1/paths/{node_id}` and path metrics, primary and alternate STUN UDP Binding responses and counters through `ipars stun probe`, plus unauthenticated relay admission rejection, authenticated relay HTTP admission, bidirectional UDP dataplane forwarding, invalid-session-credential dataplane drop accounting, and relay admission/dataplane Prometheus counters through `ipars relay probe`.
- Dedicated security, operations, and load-test plan documents cover trust boundaries, join-token controls, issuer and WireGuard key rotation, token revocation, relay abuse controls, bootstrap/join/Docker/Kubernetes runbooks, health checks, failure behavior, and 3/10/1000-node load gates.
- Daemon transport secret cleanup now revalidates tracked join-token and agent-state paths as runtime-local regular non-symlink files with the expected suffix before removal, and preserves failed cleanup targets in the harness tracking list for diagnostics instead of dropping them from memory.
- Daemon load failover reports now include survivor relay-candidate min/max and healthy/degraded/unhealthy node min/max in addition to survivor peer-map, path-state metric, and `/v1/paths/{node_id}` status checks.
- Daemon load failover also revalidates each agent status and agent `/v1/paths` endpoint after one control-plane process is stopped, preserving the original node identity, endpoint candidates, and reachable runtime path state.
- Daemon load failover sends a fresh payload through every already admitted relay UDP session after stopping one control-plane process and rejects runs where the existing relay data plane drops or corrupts that payload.

## Remaining For Full Production Completion

- Runtime backend hardening beyond current Linux command/kernel-netlink/userspace-command/dry-run selection, static configuration validation, userspace WireGuard process launch/readiness/supervision/failure-cleanup/shutdown handling and status/metrics/health lifecycle reporting, startup netlink/sysctl/capability/Docker-socket preflight, namespace path validation, command-backed child process timeout enforcement, and bounded child stdout/stderr capture, including broader integration coverage for command-backed and netlink-backed runtime preflight across realistic host privilege configurations.
- Packet-flow detector hardening beyond current typed ingestion, bounded procfs conntrack polling, bounded conntrack netlink table dumping/event subscription, bounded eBPF-exporter JSONL event ingestion with typed application hints, bounded payload-prefix samples, and existing-path regular-file/symlink checks, in-process Aya eBPF loader/ring-buffer ingestion, repository-built outbound syscall eBPF object, eBPF object/tracepoint preflight, gated privileged eBPF attach/event smoke coverage, source/protocol/port/application/payload-prefix/conntrack-status/TCP-state metadata, lifecycle classification metrics, expanded detector-hint-or-port/payload-prefix-based application metrics for common control-plane/database/observability/messaging/cache/search protocols, non-overlay destination filtering, and duplicate flow suppression, including broader privileged eBPF attach/event integration coverage and encrypted/stateful payload parsers beyond current DNS/TLS/QUIC handshake and bounded first-packet signatures.
- Privileged integration coverage beyond current namespace-aware route, WireGuard netlink, hole-punch, and relay fallback smoke tests.
- Linux namespace lifecycle/capability hardening beyond current forwarding sysctl and `CAP_NET_ADMIN`/`CAP_NET_RAW`/`CAP_SYS_ADMIN` startup preflight, `/var/run/netns` entry and `nsfs` validation, daemon preflight plus route-manager netlink and command-runner current-namespace placement warnings, smoke-script and route-manager namespace guard setup/teardown coverage, and relay-forwarder namespace identity checks, including broader lifecycle coverage across additional privileged runtime environments.
- NAT topology validation beyond current mapping/filtering probes, classification-aware signal selection, signal-generated direct-routed, endpoint-independent two-sided SNAT, two-sided fixed-port SNAT, mixed port-preserving/fixed-port two-sided SNAT, fixed-port and port-preserving one-sided public-peer SNAT traversal, signal-generated symmetric/asymmetric two-sided and one-sided address/port-dependent public-peer non-traversal, endpoint-independent SNAT namespace topologies, and broader address/port-dependent non-traversal coverage.
- Network-namespace validation of full agent path setup across additional reproducible NAT behaviours beyond the current signal-generated direct-routed, two-sided fixed-port SNAT, and one-sided public-peer SNAT traversal, fixed-port and port-preserving one-sided public-peer SNAT, one-sided address/port-dependent public-peer SNAT non-traversal, IP-only two-sided SNAT, fixed-port two-sided SNAT, mixed port-preserving/fixed-port two-sided SNAT, and symmetric/asymmetric address/port-dependent non-traversal smoke tests.
- OpenTelemetry metrics coverage beyond current control-plane node/path/health/stale-candidate/VPN-pool/join-token-ledger/peer-map ACL visibility and snapshot timestamp gauges, signal node/relay/NAT/stale-NAT/health-total/health-state/stale-health/stale-candidate/request/path-negotiation-state and snapshot timestamp metrics, STUN primary and RFC5780 alternate listener gauges plus server-side counters and snapshot timestamp gauge, relay capacity/e2e-only/session, admission, byte, packet, drop-reason counters and status snapshot timestamp gauge, and agent path/path-probe/relay-admission failure-reason/relay-forwarder/userspace-WireGuard-process/lazy-connect/packet-flow/packet-flow-duplicate-suppression/packet-flow-lifecycle/packet-flow-application/filtered-destination and snapshot timestamp metrics.
- Full rootless Docker dataplane backend support beyond current rootless socket discovery, userspace-command WireGuard peer configuration and optional process lifecycle/timeout wiring for userspace interfaces, install-plan environment wiring, Compose rootless backend/route/discovery pinning plus capability/device reset, discovery-only socket-bind/backend/process/lifecycle-timeout and non-namespaced relay-forwarder wiring, rootless rejection for namespaced relay-forwarder placement, and Unix-socket Docker API route discovery coverage with network ID/name validation and duplicate rejection, plus deeper multi-network Compose integration hardening beyond current Docker API route discovery.
- Kubernetes service/API exposure hardening beyond current least-privilege RBAC-backed Service route discovery, install-plan and chart-level route discovery validation, Helm release/namespace/join-token Secret metadata validation, chart interface-name validation, DaemonSet rollout validation, container hostNetwork/DNS policy validation, securityContext capability/privilege/read-only-root/seccomp and PodSecurityContext validation, state hostPath and mountPath validation, liveness/readiness/startup probe and preStop lifecycle validation, deeper live cluster endpoint integration coverage, relay advertisement integration coverage beyond current startup/chart validation for HTTP(S) admission/status URLs and literal public socket values, Service type, Service and target ports, NodePort uniqueness, clusterIPs, appProtocol, publishNotReadyAddresses, LoadBalancer class/source-range CIDRs and acknowledgement scope, LoadBalancer health check NodePort, LoadBalancer node-port allocation, Service IP family validation, internal/external traffic policy, traffic distribution, Service session affinity, optional NetworkPolicy CIDR ingress allowlist/source-range integration coverage beyond current scoped hostNetwork acknowledgement checks, broader annotation integration coverage, explicit Service exposure acknowledgement, source-range enforcement, and unrestricted LoadBalancer acknowledgement controls.
- Full direct path, NAT traversal, relay fallback, Docker Compose, and Kubernetes integration tests beyond current focused smoke coverage, including deeper Docker dataplane validation beyond the current gated dry-run Compose stack smoke.
- External multi-process daemon load orchestration hardening beyond current loopback `iparsd` shared-SQLite multi-control-plane/signal/RFC5780-alternate STUN/relay/dry-run-agent transport with load-option and readiness-timeout bounds validation, report-level success gates, owner-only runtime directories, scenario-matched join-token policy distribution across runtime control-plane bootstrap URLs, child liveness, private join-token file handoff with post-readiness scrubbing, post-shutdown agent-state scrubbing, retained regular runtime file owner-only hardening, temporary manifest residue rejection, unexpected retained runtime artifact rejection, retained HTTP and UDP endpoint uniqueness including STUN HTTP plus primary and alternate STUN sockets, child-role-order and duplicate/empty/role-mismatched/serial/order-skewed retained child log rejection, owner-only child log capture and log-tail diagnostics, optional retained runtime directories and owner-only atomically replaced phase-progress token-redacted run manifests with workload/options, readiness timeouts, run-start, last-update, startup-readiness, failure-refresh, intended control-plane failover, exited-child unique PID/status/code and log byte-count/log-tail hash diagnostics, service health, control-plane registration, peer-map edge, path-state/status, healthy-node, and per-endpoint min/max consistency metrics, signal health metrics, signal negotiation readiness checks, STUN HTTP metrics with alternate-listener and Binding request/response counter checks, relay JSON status and Prometheus invalid-credential drop checks, and final JSON control-plane/signal/STUN/relay capacity and admission snapshots.
