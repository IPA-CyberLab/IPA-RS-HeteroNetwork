# Generated configuration

XcodeGen writes `App-Info.plist` into this directory from `project.yml`. The
macOS app deliberately has no Network Extension, App Group, sandbox, or shared
Keychain entitlement file.
