scp -p "$HOME/.config/heterocloud/cloudflare-token" mizuame@163.220.236.51:.heterocloud-cloudflare-token && ssh -o ServerAliveInterval=30 -t mizuame@163.220.236.51 'set -euo pipefail; TOKEN="$HOME/.heterocloud-cloudflare-token"; AUTH="$(mktemp)"; WORK="$(mktemp -d)"; trap "rm -f \"$TOKEN\" \"$AUTH\"; rm -rf \"$WORK\"" EXIT; chmod 600 "$TOKEN" "$AUTH"; { printf "Authorization: Bearer "; cat "$TOKEN"; printf "\n"; } > "$AUTH"; ZONE_ID="$(curl -fsS --header "@$AUTH" "https://api.cloudflare.com/client/v4/zones?name=mizuame.app&status=active" | jq -er "if .success and (.result | length == 1) then .result[0].id else error(\"zone lookup failed\") end")"; sudo -v; if ! sudo kubectl --kubeconfig /etc/kubernetes/admin.conf -n heterocloud-dns get deployment heterocloud-dns >/dev/null 2>&1; then for LABEL in cloud-a cloud-b cloud-c flow-a flow-b flow-c rtc-a rtc-b rtc-c turn-a turn-b turn-c; do FQDN="$LABEL.heterocloud.mizuame.app"; RECORD_IDS="$(curl -fsS --header "@$AUTH" "https://api.cloudflare.com/client/v4/zones/$ZONE_ID/dns_records?type=A&name=$FQDN" | jq -r "if .success then .result[].id else error(\"record lookup failed\") end")"; for RECORD_ID in $RECORD_IDS; do curl -fsS --request DELETE --header "@$AUTH" "https://api.cloudflare.com/client/v4/zones/$ZONE_ID/dns_records/$RECORD_ID" | jq -e ".success == true" >/dev/null; done; done; fi; ASSET="heterocloud-v0.1.4-linux-x64.tar.gz"; curl -fsSLo "$WORK/$ASSET" "https://github.com/IPA-CyberLab/IPA-RS-HeteroCloud/releases/download/v0.1.4/$ASSET"; curl -fsSLo "$WORK/$ASSET.sha256" "https://github.com/IPA-CyberLab/IPA-RS-HeteroCloud/releases/download/v0.1.4/$ASSET.sha256"; (cd "$WORK" && sha256sum --check "$ASSET.sha256"); tar -xzf "$WORK/$ASSET" -C "$WORK"; sudo install -m 755 "$WORK/heterocloud" /usr/local/bin/heterocloud; sudo /usr/local/bin/heterocloud dns reconcile --domain heterocloud.mizuame.app --provider cloudflare --credential-file "CF_API_TOKEN=$TOKEN" --provider-arg="--zone-id-filter=$ZONE_ID" --public-ip 163.220.236.51 --public-ip 163.220.236.52 --public-ip 163.220.236.53 --controller-node-selector node-role.kubernetes.io/control-plane= --controller-dns-policy Default --controller-kube-api-server https://10.250.0.4:6443 --kubeconfig /etc/kubernetes/admin.conf'
