apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: heterocloud-edge
  namespace: heterocloud-edge
spec:
  gatewayClassName: heterocloud
  listeners:
    - name: http
      protocol: HTTP
      port: 18081
      allowedRoutes:
        namespaces:
          from: All
